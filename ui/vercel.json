{
  "framework": "vite",
  "installCommand": "cd .. && rm -rf node_modules ui/node_modules && bun install && node ./scripts/dev/dedupe-react-types.mjs",
  "buildCommand": "bun run build",
  "devCommand": "bun run dev",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
