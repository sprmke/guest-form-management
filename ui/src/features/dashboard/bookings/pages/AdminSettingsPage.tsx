import { AppSettingsCard } from '@/features/dashboard/bookings/components/AppSettingsCard';

/** Admin settings — property/org operational configuration. */
export function AdminSettingsPage() {
  return <AppSettingsCard />;
}
