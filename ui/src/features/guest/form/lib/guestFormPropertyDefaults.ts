/** Fallback check times when property settings are not loaded yet (matches property profile defaults). */
export const GUEST_FORM_DEFAULT_CHECK_IN_TIME = '14:00';
export const GUEST_FORM_DEFAULT_CHECK_OUT_TIME = '12:00';
