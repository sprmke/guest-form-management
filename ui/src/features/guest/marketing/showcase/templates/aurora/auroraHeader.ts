/** Aurora header type — larger than default showcase nav (`text-sm`). */
export const auroraBrandTitleClass = 'text-base font-semibold tracking-tight @sm:text-lg';

export const auroraNavLinkClass = 'text-base font-medium';
