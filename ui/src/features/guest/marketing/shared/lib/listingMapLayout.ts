/** Map canvas min-height — see `index.css` (`--listing-map-min-height`). */
export const listingMapCanvasClass = 'listing-map-canvas relative w-full overflow-hidden';

/** Error / skeleton placeholder outside the live map. */
export const listingMapMinHeightClass = 'min-h-[var(--listing-map-min-height)]';
