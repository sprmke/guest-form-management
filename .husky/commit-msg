# Strip AI co-author trailers Cursor may inject before commitlint runs.
if [ -f "$1" ]; then
  sed -i '' '/^[Cc]o-authored-by:.*[Cc]ursor/d' "$1"
  sed -i '' '/^[Cc]o-authored-by:.*cursoragent@cursor\.com/d' "$1"
fi

npx --no-install commitlint --edit "$1"
