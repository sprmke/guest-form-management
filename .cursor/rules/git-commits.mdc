---
description: Git commit authorship — never attribute commits to Cursor
alwaysApply: true
---

# Git commits — no Cursor attribution

When creating commits (only when the user explicitly asks), **never** attribute authorship to Cursor or mention Cursor in git metadata.

## Author identity

- Use the repo's existing git config (`user.name`, `user.email`) — **do not** change git config.
- **Never** pass `--author`, `GIT_AUTHOR_NAME`, `GIT_AUTHOR_EMAIL`, `GIT_COMMITTER_NAME`, or `GIT_COMMITTER_EMAIL` to impersonate Cursor or any AI tool.
- **Never** add `Co-authored-by:` trailers for Cursor, Composer, Copilot, or similar (e.g. `Co-authored-by: Cursor <cursor@cursor.com>`).

## Commit message & description

- **Never** mention Cursor, Composer, AI, or "generated by" in the subject, body, or PR description you draft.
- Write commit messages as the human developer would: what changed and why, in conventional-commit style when the repo uses it.

## Daily batch commits

Use the **`batch-commit`** skill (`.cursor/skills/batch-commit/SKILL.md`):

- **`/batch-commit N`** = **N commits per session**, each **5–10 files** — not N giant commits for the whole tree.
- Stop after N commits; leave remaining changes uncommitted for the next day.
- Never `git add -A` on a large dirty tree unless the user explicitly asks to commit everything at once.

## Examples

```text
❌ Co-authored-by: Cursor <cursoragent@cursor.com>
❌ feat: add inbox (via Cursor)
❌ 10 commits × 200 files each (whole refactor in one day)

✅ feat: add guest inbox thread list (4 files)
✅ /batch-commit 10 → 10 small commits today, ~50–100 files total
```

## Repo enforcement

`.husky/commit-msg` strips `Co-authored-by: … Cursor …` trailers before commitlint runs.
