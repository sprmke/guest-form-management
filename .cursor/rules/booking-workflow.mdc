---
description: Booking status state machine, transition side effects, and the "which file do I edit?" map for the new booking flow
globs:
  - 'supabase/functions/_shared/statusMachine.ts'
  - 'supabase/functions/_shared/workflowOrchestrator.ts'
  - 'supabase/functions/_shared/emailService.ts'
  - 'supabase/functions/_shared/email-templates/**/*'
  - 'supabase/functions/transition-booking/**/*'
  - 'supabase/functions/submit-form/**/*'
  - 'supabase/functions/gmail-listener/**/*'
  - 'supabase/functions/approval-email-webhook/**/*'
  - 'supabase/functions/reconcile-document-approvals/**/*'
  - 'supabase/functions/sd-refund-cron/**/*'
  - 'supabase/functions/send-sd-refund-form-email/**/*'
  - 'supabase/functions/parking-broadcast-email/**/*'
  - 'supabase/functions/get-sd-form/**/*'
  - 'supabase/functions/submit-sd-form/**/*'
  - 'supabase/functions/claim-sd-voucher/**/*'
  - 'supabase/functions/upload-booking-asset/**/*'
  - 'supabase/functions/list-bookings/**/*'
  - 'supabase/functions/cancel-booking/**/*'
  - 'ui/src/features/dashboard/bookings/**/*'
  - 'ui/src/features/guest/form/**/*'
  - 'ui/src/features/guest/sd-form/**/*'
  - '.cursor/skills/booking-workflow/**/*'
  - '.agent/skills/booking-workflow/**/*'
alwaysApply: false
---

# Booking workflow — agent reference

This rule is the single source of truth for the **new booking lifecycle** described in `docs/archive/planning/NEW_FLOW_PLAN.md` (plan) and `docs/archive/planning/NEW_FLOW.md` (original spec).

Whenever a task touches **booking state, emails, PDF persistence, or the admin dashboard**, start here.

## 1. Status enum (canonical)

Use the string literals below in DB, code, and UI. **Postgres storage:** `TEXT` + `CHECK (status IN (...))` — see `docs/archive/planning/NEW_FLOW_PLAN.md` §6.1 **Q1.3** (avoid Postgres `ENUM` churn). Keep a single source (`supabase/functions/_shared/statusMachine.ts` on the server, `ui/src/features/dashboard/bookings/lib/workflow.ts` on the client — keep in sync).

```
PENDING_REVIEW
PENDING_DOCUMENTS
PENDING_GAF
PENDING_PARKING_REQUEST
PENDING_PET_REQUEST
READY_FOR_CHECKIN
READY_FOR_CHECKOUT
PENDING_SD_REFUND
COMPLETED
CANCELLED
IMPORTED
```

> **`IMPORTED`** — reached only by **`import-commit`** direct insert (Phase 6), not via `workflowOrchestrator.transition()` or the normal pipeline. Manual admin overrides: **`IMPORTED → CANCELLED`** (for import revert) or **`IMPORTED → PENDING_REVIEW`** (pull a row into the live pipeline). Not in `TERMINAL_STATUSES` — unlike `COMPLETED` / `CANCELLED`, it retains manual-outgoing edges.

> Legacy DB rows have `status = 'booked'` (or `'canceled'`). The migration maps them per **`docs/archive/planning/NEW_FLOW_PLAN.md` §6.1 Q1.1**: check-in **before today (Asia/Manila)** → `COMPLETED`; check-in **today or future** → `PENDING_REVIEW`; canceled rows → `CANCELLED`.

## 2. Allowed transitions

### 2.1 Primary workflow (happy path + cancel)

```
PENDING_REVIEW              → PENDING_DOCUMENTS | CANCELLED
PENDING_DOCUMENTS           → PENDING_DOCUMENTS | READY_FOR_CHECKIN | CANCELLED
PENDING_GAF                 → PENDING_DOCUMENTS | READY_FOR_CHECKIN | CANCELLED
PENDING_PARKING_REQUEST     → PENDING_DOCUMENTS | READY_FOR_CHECKIN | CANCELLED
PENDING_PET_REQUEST         → PENDING_DOCUMENTS | READY_FOR_CHECKIN | CANCELLED
READY_FOR_CHECKIN           → READY_FOR_CHECKOUT | CANCELLED
READY_FOR_CHECKOUT          → PENDING_SD_REFUND | COMPLETED | CANCELLED
PENDING_SD_REFUND           → COMPLETED | CANCELLED
COMPLETED                   → (terminal)
CANCELLED                   → (terminal)
IMPORTED                    → (no automatic progression — import-insert only)
```

**Manual override from `IMPORTED`:**

```
IMPORTED                    → CANCELLED | PENDING_REVIEW   # admin-only; CANCELLED for import-revert
```

### 2.2 Admin-only exceptional transitions

These exist so humans can recover when automation lags, guest data changes late, or the admin needs to step the booking back one stage to redo a step.

**Forward-only override** (force-advance, e.g. when the Gmail listener / cron is skipped):

```
PENDING_GAF             → READY_FOR_CHECKIN   # approved GAF uploaded manually, no parking/pet sub-stages
READY_FOR_CHECKIN       → PENDING_SD_REFUND   # skip guest SD form stage (manual); no SD form email on this edge
```

**Backward overrides** (one-step-back recovery — the WorkflowPanel surfaces these as a "Back to …" button):

```
PENDING_DOCUMENTS           → PENDING_REVIEW
PENDING_GAF                 → PENDING_DOCUMENTS | PENDING_REVIEW
PENDING_PARKING_REQUEST     → PENDING_DOCUMENTS | PENDING_REVIEW
PENDING_PET_REQUEST         → PENDING_DOCUMENTS | PENDING_REVIEW
READY_FOR_CHECKIN           → PENDING_DOCUMENTS | PENDING_PET_REQUEST | PENDING_PARKING_REQUEST | PENDING_GAF | PENDING_REVIEW | PENDING_SD_REFUND
READY_FOR_CHECKOUT          → READY_FOR_CHECKIN
PENDING_SD_REFUND           → READY_FOR_CHECKOUT
```

The override **graph** is permissive — it lists every back-step that is structurally valid. The **UI** narrows it down via `bookingPipeline()` to a single "Back to …" button that always points at the booking's _immediately previous_ applicable step (e.g. parking is skipped when `need_parking = false`).

**Email safety:** backward transitions never fire outbound email. The orchestrator sends the ready-for-check-in email only on **forward** moves to `READY_FOR_CHECKIN` from `PENDING_REVIEW` (D2 empty-requirements skip), `PENDING_DOCUMENTS` (parent docs complete), or legacy `PENDING_GAF` / `PENDING_PARKING_REQUEST` / `PENDING_PET_REQUEST` — not from e.g. `PENDING_SD_REFUND` → `READY_FOR_CHECKIN`. The **SD refund form request** email is gated on `fromStatus === READY_FOR_CHECKIN && toStatus === READY_FOR_CHECKOUT`, so stepping back from `READY_FOR_CHECKOUT` to `READY_FOR_CHECKIN` does not re-send; admins can re-send from **Automation Triggers** while the booking stays on `READY_FOR_CHECKOUT`.

**SD refund skip (security_deposit=0):** When `security_deposit` is 0 (typical for Airbnb bookings where DP/SD default to 0), the `PENDING_SD_REFUND` step is skipped entirely — `READY_FOR_CHECKOUT` goes directly to `COMPLETED`. The SD refund form request email is not sent, the guest does not see the `/sd-form`, and `sd-refund-cron` suppresses the check-out email for SD=0 bookings. The UI pipeline (`bookingPipeline`) hides `PENDING_SD_REFUND` when `security_deposit === 0`.

**Airbnb source behavior:** Airbnb bookings (`booking_source === 'Airbnb'`, set via `?source=airbnb` on the guest form or admin **Booking Source** edit) differ from Facebook bookings: (1) guest form skips the Payment step (4 steps instead of 5), `paymentReceipt` is not required; (2) `ReviewPricingForm` defaults DP and SD to **₱0** (editable by admin if needed); (3) admin `BookingEditForm` hides the Downpayment receipt document row; (4) `ready-for-checkin` email hides the Payment breakdown + GCash QR section when total balance due is 0 (source-agnostic — applies to any booking with zero total balance); (5) `submit-form` skips downpayment receipt AI validation for Airbnb; (6) **guest balance settlement** excludes booking rate, down payment, and security deposit from total guest balance — only pet fee + additional guest fee count; receipt AI applies only when that fee total is **> ₱0**. Switching source to Airbnb in admin edit zeros DP/SD and clears stale guest balance settlement fields.

**Manual "force advance"** (workflow panel): admins must be able to trigger transitions that would normally be fired by **cron** or the **Gmail listener** when those automations are late or skipped.

Implementation rule: keep **two layers** in `statusMachine.ts`:

1. `canTransition(from, to)` — strict graph for automation + normal admin clicks.
2. `canManualForceTransition(from, to, ctx)` — **small explicit allow-list** of extra edges (e.g. `READY_FOR_CHECKIN → PENDING_SD_REFUND` even before cron, `PENDING_GAF → READY_FOR_CHECKIN` when the approved PDF was uploaded manually, etc.). **Never** accept arbitrary `{ toStatus }` from the client without this second check.

`transition-booking` always calls the orchestrator with `{ manual: true | false }` so the server can tell which validator ran.

The server **must** validate every transition before writing. **Manual advances** and **cron/listener** calls must all go through `workflowOrchestrator.transition()` — never duplicate side-effect logic in callers.

### 2.3 Guest-field edit → revert to review

Only revert `status` back to `PENDING_REVIEW` when both are true:

1. The update came from one of these flows:
   - Public guest update (`/form?bookingId=...`)
   - Admin booking-detail save (`/bookings/:bookingId` edit form → `guest_submissions` patch)
   - Admin guest-document replacement (`upload-booking-asset` for `payment_receipt` | `valid_id` | `pet_vaccination` | `pet_image` only — other asset types do not revert)
2. At least one workflow-sensitive field changed:
   - Facebook / Airbnb Name
   - Primary Guest name
   - Email
   - Phone Number
   - Additional Guest Names
   - Check-in and Check-out Date
   - Check-in and Check-out Time
   - Parking Details
   - Surprise decor request (`guest_requests_surprise_decor`)
   - Pet Details — only sensitive when the property's resolved document requirements include a `has_pets`-triggered requirement or id `pet` (§4.5)
   - Downpayment receipt
   - Valid ID
   - Pet vaccination and pet photo

If only non-listed fields are updated, keep the current status (do **not** revert).

When a listed field changes while the booking’s `status` is one of **`PENDING_DOCUMENTS`**, **`PENDING_GAF`**, **`PENDING_PARKING_REQUEST`**, **`PENDING_PET_REQUEST`**, or **`READY_FOR_CHECKIN`**, set `status` to `PENDING_REVIEW` (and update `status_updated_at`). In the **same write**, apply **`statusMachine.ts#pendingDocumentsClearPatchForGuestEditRevert`**: nested completion timestamps, manual-incomplete flags, **approved** PDF URLs, **admin parking settlement** columns, and **guest balance settlement** columns — **not** pricing snapshot fields (`booking_rate`, `down_payment`, `balance`, `security_deposit`, `pet_fee`, `parking_rate_guest`, `guest_additional_fee`) and **not** request PDF URLs (`gaf_request_pdf_url`, `pet_request_pdf_url`) unless PDF fill content changed (`requestPdfClearPatchForChangedFormFields` / admin mirror `requestPdfClearPatchForAdminGuestEdit`). Guest doc uploads (`valid_id`, `pet_vaccination`, `pet_image`) revert status but **preserve** stored request PDFs. **`workflowOrchestrator.ts`** merges the **same patch** at the start of **`PENDING_REVIEW → PENDING_DOCUMENTS | PENDING_GAF`** before payload pricing, preventing stale `*_completed_at` from showing substeps as complete immediately after **Proceed to Pending Documents** without wiping stored pricing unless the payload supplies new values. Proceed then **regenerates** request PDFs when `generatePdf` is on, reusing the prior URL when generation fails, and **throws** if a required request PDF is still missing after proceed. Do **not** apply this revert when status is already **`PENDING_REVIEW`**, in **`READY_FOR_CHECKOUT`** / **`PENDING_SD_REFUND`**, **`COMPLETED`**, or **`CANCELLED`**. Server helper: `statusMachine.ts#shouldRevertGuestFieldEditsToPendingReview`.

**Dev-control checkboxes** on the guest form (save DB, storage, email) still apply to **every** submit/save — reverting status never bypasses them. **PDF generation** is not part of guest submit; it runs from the orchestrator on `PENDING_REVIEW` → initial documents when property automations and resolved requirements allow.

## 3. Side-effect matrix

Each transition fans out a known set of side effects. Implement them inside `_shared/workflowOrchestrator.ts` and call it from the `transition-booking` edge function.

| From → To                                                          | DB                                                                                                                                                                                                                                                                                                    | Storage                  | Email(s)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| (guest submit) → PENDING_REVIEW                                    | ✅                                                                                                                                                                                                                                                                                                    | ✅ (uploads only)        | **New Booking Request** to **`EMAIL_REPLY_TO`** when **`sendEmail` ≠ `false`** (`submit-form` → `sendNewBookingRequestNotify`; **not** orchestrator / not workflow acknowledgement)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| PENDING_REVIEW → PENDING_GAF **or** `PENDING_DOCUMENTS`            | ✅                                                                                                                                                                                                                                                                                                    | ✅ request PDFs + URLs   | GAF request (Azure, **with filled GAF PDF + all guest valid ID images on file**), booking acknowledgement (guest), pet request if pets (**with pet PDF**), parking broadcast if parking                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| PENDING_GAF → PENDING_PARKING_REQUEST                              | ✅                                                                                                                                                                                                                                                                                                    | ✅ save approved GAF     | ❌                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| PENDING_GAF → PENDING_PET_REQUEST                                  | ✅                                                                                                                                                                                                                                                                                                    | ✅ save approved GAF     | ❌                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| PENDING_GAF → READY_FOR_CHECKIN                                    | ✅                                                                                                                                                                                                                                                                                                    | ✅ save approved GAF     | Ready-for-check-in (guest)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| PENDING_PARKING_REQUEST → PENDING_PET_REQUEST                      | ✅                                                                                                                                                                                                                                                                                                    | ✅ parking endorsement   | ❌                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| PENDING_PARKING_REQUEST → READY_FOR_CHECKIN                        | ✅                                                                                                                                                                                                                                                                                                    | ✅ parking endorsement   | Ready-for-check-in (guest)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| PENDING_PET_REQUEST → READY_FOR_CHECKIN                            | ✅                                                                                                                                                                                                                                                                                                    | ✅ save approved pet PDF | Ready-for-check-in (guest)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| READY_FOR_CHECKIN → READY_FOR_CHECKOUT (cron / manual)             | ✅ (persists `guest_balance_paid_amount`, `guest_balance_payment_receipt_url` from payload; paid must **equal** **computed total guest balance** — same formula as pricing card, not the `balance` column alone; **receipt required only when total > ₱0**; unset paid treats as ₱0 when total is ₱0) | —                        | **Check-out & SD Refund** guest email: `sd-refund-cron` sends it when the pre-check-out **lead** window opens (`SD_REFUND_CRON_EMAIL_LEAD_MINUTES`, default 2h), **without** waiting on settlement (idempotent via `sd_refund_form_emailed_at`). The **transition** to `READY_FOR_CHECKOUT` still requires settlement. On transition, `sendSdRefundFormEmail` is **off** if the guest was already emailed. Stale check-outs: **`sd-refund-cron`** suppresses **both** automated email and transition-time email when older than `SD_REFUND_CRON_MAX_CHECKOUT_AGE_DAYS` (default 30d, `0` = never suppress). Admin scoped cron (`POST` `{ bookingId }` + JWT) evaluates **one** row; scheduled cron scans **all** `READY_FOR_CHECKIN` |
| READY_FOR_CHECKOUT → PENDING_SD_REFUND (guest `/sd-form` or admin) | ✅                                                                                                                                                                                                                                                                                                    | —                        | ❌ — persists guest refund fields when payload includes `sd_refund_method`; admin skip (no method) omits those columns. **Independent of the transition**, `claim-sd-voucher` may persist `next_stay_voucher_*` while status is `READY_FOR_CHECKOUT` (no orchestrator pass — read-only side path)                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| PENDING_SD_REFUND → COMPLETED                                      | ✅                                                                                                                                                                                                                                                                                                    | —                        | ❌ (or send refund receipt if product wants — confirm)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| any → CANCELLED                                                    | ✅                                                                                                                                                                                                                                                                                                    | —                        | ❌ (keep existing cancel behavior)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

Rules:

- **Never CC the guest** on the GAF-request and pet-request emails to Azure.
- **The guest acknowledgement email** (to guest) is separate and only goes out on `PENDING_REVIEW → PENDING_GAF`, **`PENDING_REVIEW → PENDING_DOCUMENTS`**, or **`PENDING_REVIEW → READY_FOR_CHECKIN`** (D2 empty-requirements skip) — same bundle as GAF request / pet / parking.
- The **parking broadcast email** uses BCC to all addresses in `PARKING_OWNER_EMAILS` (comma-separated env var; initial seed list documented in `docs/archive/planning/NEW_FLOW_PLAN.md` §6.1 Q4.1).
- **Parking owner replies** are **manual** in v1 — no Gmail listener for parking threads.
- **GAF/pet mark-complete:** admin **Mark … complete** opens a modal to upload or confirm the approved PDF (`upload-booking-asset` → `approved_gaf` / `approved_pet`, or inbound `approval-email-webhook`). `workflowOrchestrator` **throws** if `document_completion_target` targets a GAF/pet template requirement without `approved_gaf_pdf_url` / `approved_pet_pdf_url`. Nested completion for those requirements counts only when **`approvedPdfUrl`** is on file — `completedAt` alone is not enough.
- **Parking nested completion:** `parking_endorsement_url` (upload) alone does **not** mark the parking sub-step complete in the admin stepper. **`parking_completed_at`** is set only when the admin confirms **Mark as Complete — Pending Parking Request** (`document_completion_target: 'PENDING_PARKING_REQUEST'` on `transition-booking`).
- **Late parking (RFCI+):** When **`need_parking`** is added or still incomplete at **`READY_FOR_CHECKIN`** or later, the **Pending Parking Request** nested sub-step stays **clickable** in the stepper (unless already complete). Admin marks complete via same-status `transition-booking` (`toStatus` = current status, `document_completion_target: 'PENDING_PARKING_REQUEST'`) — **no** revert to **`PENDING_DOCUMENTS`**.
- **Mark nested docs incomplete:** `PENDING_DOCUMENTS` → same status with **`document_completion_clear_target`** (admin-only) clears **`parking_completed_at`**, or writes `{ completedAt: null, manualIncomplete: true }` for the target requirement id in **`document_requirement_completions`** so the sub-step shows incomplete without deleting **`approvedPdfUrl`** / **`approved_*_pdf_url`**. **`approval-email-webhook`** (new approval) and **`document_completion_target`** clear the manual-incomplete flag when completing again. Both targets resolve through the property's requirement list — see §4 **Completion writes**.
- **`reconcile-document-approvals`:** admin (or auto on Pending Documents load) re-runs **`WorkflowOrchestrator`** for **`PENDING_DOCUMENTS`** rows that still have **`approved_gaf_pdf_url` / `approved_pet_pdf_url`** but **`gaf_manual_incomplete` / `pet_manual_incomplete`** is true — so mark-incomplete can be restored without a new Azure email.

## 4. Configurable document requirements (D3)

The nested docs under `PENDING_DOCUMENTS` are **data-driven**, not hardcoded GAF/pet literals. A per-property `DocumentRequirement[]` list (`{ id, label, order, pdfTemplateId, approvalSource: 'manual'|'email-listener', triggerCondition: 'always'|'has_pets'|'need_parking', calendarIcon }`) drives the stepper, WorkflowPanel sub-form, and sensitive-diff gating. Shared types live in `_shared/documentRequirements.ts` (server) mirrored by `ui/.../lib/documentRequirements.ts` (client) — keep both in lockstep, same rule as `statusMachine.ts`.

**Resolution order** (per property): stored `documentRequirementsOverride` (`null` = inherit) → residence-type default → `DEFAULT_DOCUMENT_REQUIREMENTS` (today: `gaf` always, `pet` on `has_pets`). The `app-settings` GET returns both `resolvedDocumentRequirements` (what the UI renders) and `residenceDefaultDocumentRequirements` (for the settings preview). **Parking stays hardcoded outside this list** — it is never a `DocumentRequirement`, only a fixed `PENDING_PARKING_REQUEST` sub-step gated on `need_parking`.

**Nested tree order:** `requirements.filter(requirementApplies).sort(by order)`, with the Parking row inserted immediately before the first `has_pets`-triggered requirement (same insertion point `pendingDocumentsNestedItems` uses) — mirrors the existing GAF → Parking → Pet display order when using the defaults.

**Completion dual-read:** `readDocumentCompletions(booking)` prefers the JSONB `document_requirement_completions` map (`{ [requirementId]: { completedAt, approvedPdfUrl, manualIncomplete } }`); when an id has no map entry, it falls back to the legacy named columns (`gaf_completed_at`/`approved_gaf_pdf_url`/`gaf_manual_incomplete`, `pet_*`) so already-in-flight bookings keep working during the transition. `document_completion_target` / `document_completion_clear_target` on `transition-booking` accept either a bare requirement id (`"gaf"`, `"custom-2"`, …) or the legacy `PENDING_GAF`/`PENDING_PET_REQUEST` literals (normalized via `LEGACY_DOC_TARGET_TO_REQUIREMENT_ID`); parking keeps using the `PENDING_PARKING_REQUEST` literal.

**Completion writes (`workflowOrchestrator.ts#resolveDocTarget`):** completion is written **per requirement id** into the JSONB map — never against a hardcoded `gaf`/`pet` branch, or a property that renamed its GAF requirement (e.g. id `custom-2`) would get a successful `transition-booking` response that persisted nothing. Resolution order: exact id in the property's resolved list → for a legacy literal, the requirement whose `pdfTemplateId` matches (`gaf`/`pet`) → legacy id itself (named columns only, logged as a warning, so `gmail-listener` never hard-fails on a renamed list). A target matching none of these **throws** — mark-complete must never report success without a write. `gaf`/`pet` additionally dual-write their named columns. Same rule for the guest-edit revert: `pendingDocumentsClearCompletionsJsonbPatch` clears **every** id in the map, not just `gaf`/`pet`.

**Renaming a requirement id** (override or residence default) therefore keeps admin mark-complete working, but Gmail-driven approval only tracks it when that requirement's **`pdfTemplateId`** is `gaf` / `pet` — the listener has no other way to link an inbox approval to a custom id.

**D2 — empty requirements:** when a property's resolved list is `[]` (explicit override, no GAF/pet needed), `PENDING_DOCUMENTS` is skipped entirely from the pipeline — `bookingPipeline()` (client) and the orchestrator (server) both route `PENDING_REVIEW → READY_FOR_CHECKIN` directly. The transition graph already allows this edge (`§2.1`); the client just never shows a "Proceed to Pending Documents" button when `documentRequirements.length === 0`. Pricing (`ReviewPricingForm`) is still required on this direct skip — same as any other `PENDING_REVIEW` proceed attempt.

**Sensitive-diff gating (admin edit form only):** in **`BookingEditForm.tsx`** (`workflowSensitiveGuestDiff.ts#petDocumentationConfigured`), Pet Details (toggle + name/type/breed/age/vaccination date) are only workflow-sensitive (§2.3) when the resolved requirements include a `has_pets`-triggered requirement or the literal id `pet` — a property with no pet documentation configured does not revert on pet-field edits from the admin edit form. Parking sensitivity is unaffected. The **public guest form** (`submit-form` → `_shared/utils.ts#WORKFLOW_SENSITIVE_FORM_FIELDS`) and **`upload-booking-asset`** revert paths are **not** gated by this — pet fields there are unconditionally sensitive regardless of resolved document requirements.

**Outbound emails on proceed:** GAF/pet PDFs and request emails run when the resolved requirements include an applicable requirement whose **`pdfTemplateId` or `id`** matches `gaf` / `pet` (`hasApplicableDocumentPdfTemplate`). Each proceed **regenerates** request PDFs (upsert + `?v=` on the stored URL); generation/upload failures **throw** and block the transition. Booking acknowledgement and parking broadcast (when `need_parking`) apply to **any** `PENDING_REVIEW` proceed attempt, including the D2 direct skip to `READY_FOR_CHECKIN`. Each is additionally gated by the property's **Email automations** toggles (`propertyAutomationToggles.ts`).

## 5. "Where do I edit this?" map

When the task is…

| Concern                                              | File / folder                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Status enum, transition graph                        | `supabase/functions/_shared/statusMachine.ts` (create) + mirror in `ui/src/features/dashboard/bookings/lib/workflow.ts`                                                                                                                                                                                  |
| CSV import batches (status machine, edge functions)  | `supabase/functions/_shared/importBatchStatusMachine.ts` + mirror `ui/src/features/dashboard/import/lib/importBatchWorkflow.ts`; edge handlers `import-*` (Phase 2+)                                                                                                                                     |
| Apply side effects for a transition                  | `supabase/functions/_shared/workflowOrchestrator.ts` (create)                                                                                                                                                                                                                                            |
| Transition endpoint                                  | `supabase/functions/transition-booking/index.ts` (create)                                                                                                                                                                                                                                                |
| Admin booking list endpoint                          | `supabase/functions/list-bookings/index.ts` (create)                                                                                                                                                                                                                                                     |
| Gmail approval intake (production)                   | `supabase/functions/approval-email-webhook/index.ts` + `_shared/approvalEmailMatcher.ts` — Resend Receiving `email.received`; plus-address property routing; **`processed_emails`** dedupe. Ops: **`docs/archive/operations/approval-email-inbound.md`**                                                 |
| Reconcile stored GAF/pet after mark-incomplete       | `supabase/functions/reconcile-document-approvals/index.ts` + `_shared/approvalDocumentReconcile.ts` — admin JWT; also auto on Pending Documents load                                                                                                                                                     |
| SD refund cron                                       | `supabase/functions/sd-refund-cron/index.ts` (create)                                                                                                                                                                                                                                                    |
| Guest SD refund form (public)                        | `supabase/functions/get-sd-form`, `submit-sd-form`; UI `ui/src/features/guest/sd-form/**`, route `/sd-form?bookingId=`                                                                                                                                                                                   |
| Admin **READY_FOR_CHECKOUT**: SD link + recheck      | `ui/src/features/dashboard/bookings/components/WorkflowPanel.tsx` — **SD Refund Link** + **`InlineCopyIconButton`**; **Check for guest submission** refetches `BOOKING_QUERY_KEY` (`useBooking`)                                                                                                         |
| Re-send SD form email (admin)                        | `supabase/functions/send-sd-refund-form-email/index.ts`                                                                                                                                                                                                                                                  |
| Next-stay Facebook-review voucher                    | `supabase/functions/claim-sd-voucher/index.ts` + `_shared/voucher.ts` (server allow-list / `VOUCHER_WIN_POOL`); UI `ui/src/features/guest/sd-form/components/VoucherReveal.tsx` + `lib/voucher.ts` (catalog mirror); admin display in `BookingDetailPage` Pricing card                                   |
| Parking broadcast email                              | `supabase/functions/parking-broadcast-email/index.ts` (create)                                                                                                                                                                                                                                           |
| Guest submit + **New Booking Request** notify        | `submit-form/index.ts` + `email-templates/new-booking-request.html` + `emailService.ts#sendNewBookingRequestNotify` — after successful DB save when **`sendEmail` ≠ `false`**, **To:** `EMAIL_REPLY_TO` only; guest dev **Send email** maps to the query param                                           |
| New email templates                                  | `supabase/functions/_shared/email-templates/*.html` + `replacePlaceholders` in `emailService.ts`                                                                                                                                                                                                         |
| DB columns                                           | `supabase/migrations/2026…*.sql` (add new migrations — never edit shipped ones)                                                                                                                                                                                                                          |
| Overlap check (non-CANCELLED blocks)                 | `supabase/functions/_shared/databaseService.ts` → `checkOverlappingBookings`                                                                                                                                                                                                                             |
| Admin UI (auth + list + detail)                      | `ui/src/features/dashboard/bookings/**` (create); workflow rail sub-form shell: `WorkflowSubFormCard.tsx`                                                                                                                                                                                                |
| Pending review: confirm guest form before rail       | `hooks/usePendingReviewAck.ts` + `components/workflow-panel/WorkflowPendingReviewAck.tsx` — `WorkflowPanel` keeps the stage deck header and swaps the body + actions bar for the confirmation while `status === PENDING_REVIEW`                                                                          |
| Past stay: warn before Proceed (Manila dates)        | `ui/src/features/dashboard/bookings/lib/bookingPastPipelineManila.ts` + `WorkflowPanel` confirm modal banner when check-in or check-out is before today                                                                                                                                                  |
| Pipeline + RFCI revert (whitelist) on admin save     | `ui/src/features/dashboard/bookings/lib/workflowSensitiveGuestDiff.ts` + `bookingStatus.ts#shouldRevertGuestFieldEditsToPendingReview` + `pendingDocumentsClearPatchForGuestEditRevert` + `BookingEditForm.tsx` → `BookingEditSaveChoiceDialog` → `useUpdateBooking` (admin chooses revert vs save-only) |
| Stepper nested docs under Pending Review             | `ui/src/features/dashboard/bookings/lib/workflow.ts#isSubStatusCompletedInStepper` (`WorkflowPanel` `PendingDocumentsSubTree`) — no green checks on GAF/parking/pet while `status === PENDING_REVIEW`                                                                                                    |
| Pipeline + RFCI revert on guest doc upload           | `supabase/functions/upload-booking-asset/index.ts` (guest doc `assetType`s only) + `statusMachine.ts#shouldRevertGuestFieldEditsToPendingReview`                                                                                                                                                         |
| Guest form non-prod API toggles                      | `ui/src/features/guest/form/components/GuestForm.tsx` + `CalendarPage.tsx` + `bookingSourceFromSearchParams.ts#stripLegacyFromQueryParam`                                                                                                                                                                |
| Configurable document requirements (types + default) | `supabase/functions/_shared/documentRequirements.ts` (server) + `ui/src/features/dashboard/bookings/lib/documentRequirements.ts` (client mirror)                                                                                                                                                         |
| Nested-doc stepper items + completion + D2 skip      | `ui/src/features/dashboard/bookings/lib/workflow.ts` (`pendingDocumentsNestedItems`, `getPendingDocumentsNestedCompletion`, `bookingPipeline`) — server mirror in `_shared/statusMachine.ts`                                                                                                             |
| Property override + resolved list                    | `supabase/functions/app-settings/index.ts` (GET returns `resolvedDocumentRequirements` / `residenceDefaultDocumentRequirements`); UI edit in property Settings page                                                                                                                                      |
| Property email automation toggles                    | `supabase/functions/_shared/propertyAutomationToggles.ts` + `ui/src/features/dashboard/org/components/property-settings/PropertyEmailAutomationsSection.tsx`                                                                                                                                             |

## 6. Invariants (tests should cover these)

- `submit-form` never calls Azure GAF `sendEmail`, `sendPetEmail`, or **`generatePDF`** / **`generatePetPDF`** (no guest-submit PDFs). After a successful **DB** save, when the **`sendEmail` query param is not `false`**, it calls **`sendNewBookingRequestNotify`** (owner/ops alert to **`EMAIL_REPLY_TO` only** via `new-booking-request.html`) — not workflow acknowledgement / GAF / pet / parking.
- Only `workflowOrchestrator.transition()` mutates `status` and `status_updated_at` for **workflow transitions** (admin/cron/listener). **Exception — pipeline / `READY_FOR_CHECKIN` → `PENDING_REVIEW` safety revert** (no orchestrator): `submit-form` when workflow-sensitive guest fields change while `shouldRevertGuestFieldEditsToPendingReview(status)`, admin `guest_submissions` patch from `BookingEditForm` under the same rule, and `upload-booking-asset` for guest doc `assetType`s `payment_receipt` | `valid_id` | `pet_vaccination` | `pet_image`. These paths set `status`, `status_updated_at`, and **`pendingDocumentsClearPatchForGuestEditRevert()`** fields in one row update; they do not send workflow emails or call the orchestrator.
- `get-booked-dates` returns everything where `status <> 'CANCELLED'` (not just `'booked'`).
- Inbound approval emails are processed **at most once** (idempotency via `processed_emails` keyed on Resend `email_id`).
- Guest SD refund submissions use **`submit-sd-form`** → `WorkflowOrchestrator.transition()` (same invariants as admin transitions); **`get-sd-form`** is read-only and returns **404** unless status is `READY_FOR_CHECKOUT` **or** `READY_FOR_CHECKIN` with `sd_refund_form_emailed_at` set (returns `awaiting_balance_settlement` until status advances).

## 7. Don'ts

- Don't re-introduce `?testing=true` or a separate test-booking pipeline in UI or edge code (use staging/local Supabase instead).
- After the migration ships, **application code** should treat `status` as the **new canonical literals only**. The migration itself may still read legacy `'booked'` / `'canceled'` — that's OK, but delete dead branches once backfill is done.
- Don't put email calls inside `DatabaseService`; keep services single-responsibility.
- Don't add side effects inline to transition endpoints — go through the orchestrator so every caller (UI, Gmail listener, cron) behaves identically.
- Don't forget to update `docs/PROJECT.md` and `docs/todos/` (GitHub Issues + epic specs) per `documentation-maintenance.mdc`.

## 8. Related docs & rules

- `docs/archive/planning/NEW_FLOW_PLAN.md` — full plan, **§6.1 decisions**, **§6.2** minor refinements, phased rollout.
- `docs/archive/planning/NEW_FLOW.md` — original product spec.
- `docs/PROJECT.md` — current architecture (pre-redesign).
- `.cursor/rules/admin-auth.mdc` — admin auth specifics.
- `.cursor/rules/supabase-edge-functions.mdc` — edge function conventions.
- `.cursor/skills/booking-workflow/SKILL.md` — operational walkthrough.
- `.cursor/skills/gmail-listener/SKILL.md` — Gmail OAuth + history polling + idempotency.
- `.cursor/skills/admin-dashboard/SKILL.md` — list + detail page patterns.
