---
description: Admin authentication (Supabase Google OAuth + allow list) and dev-mode rules for the new /bookings dashboard
globs:
  - 'supabase/functions/_shared/auth.ts'
  - 'supabase/functions/_shared/orgAuth.ts'
  - 'supabase/functions/_shared/propertyScope.ts'
  - 'supabase/functions/_shared/parkingScope.ts'
  - 'supabase/functions/_shared/serveEdge.ts'
  - 'ui/src/lib/supabase/**/*'
  - 'ui/src/features/dashboard/bookings/components/RequireAdmin.tsx'
  - 'ui/src/features/dashboard/org/**/*'
  - 'ui/src/features/dashboard/team/**/*'
  - 'ui/src/features/guest/form/components/GuestForm.tsx'
  - 'ui/src/features/guest/marketing/**/login*'
  - 'ui/src/features/guest/marketing/**/Login*'
  - '.cursor/skills/supabase-auth/**/*'
  - '.agent/skills/supabase-auth/**/*'
alwaysApply: false
---

# Admin auth — agent reference

Applies whenever a change touches the `/bookings` routes, the new admin-only edge functions, or guest-form non-production API toggles.

## 1. Auth model

- Provider: **Supabase Auth** with **Google OAuth**.
- Client: `@supabase/supabase-js` (add to `ui/package.json` when implementing).
- Single shared Supabase client: `ui/src/lib/supabase/client.ts`.
- Entry point: **`/for-hosts/login`** — calls `supabase.auth.signInWithOAuth({ provider: 'google' })`. Legacy **`/sign-in`** redirects here.

## 2. Allow list (server)

- **`ADMIN_ALLOWED_EMAILS`** (edge only) — comma-separated emails for `verifyAdminJwt` and `isPlatformAdmin`. Org **owners** can pass `verifyAdminJwt` without being on this list.
- **`SUPER_ADMIN_EMAILS`** (edge only) — platform `/admin/*` APIs (`serveSuperAdmin`, `list-developments`, …).
- **`VITE_SUPER_ADMIN_EMAILS`** (UI / Vercel) — client UX gate for the Admin mode tab and `RequireSuperAdmin`; **not** the security boundary.
- There is **no** `VITE_ADMIN_ALLOWED_EMAILS` — host dashboard access is Google sign-in + org/team RBAC; do not reintroduce a client allow list.

## 3. Route guards (UI)

- `RequireAdmin.tsx` — loader while session hydrates; redirects to **`/for-hosts/login`** if unsigned. **No client email allow list.**
- `RequireSuperAdmin.tsx` — checks `VITE_SUPER_ADMIN_EMAILS` (UX only); server enforces `SUPER_ADMIN_EMAILS`.
- Applied to org/property dashboard routes via `RequireAdmin`; `/admin/*` via `RequireSuperAdmin`.

## 4. Guest form dev controls (`GuestForm.tsx`)

- **Production (`VITE_NODE_ENV === 'production'`):** no dev checkbox panel; submits use full save + storage + calendar + sheet flags as wired in `submit-form`, and **New Booking Request** email runs by default (`sendEmail` omitted → treated as on server-side).
- **Non-production:** dev panel shows save/storage/calendar/sheet/**Send email** toggles (**all on by default**; uncheck to skip). Flags are sent as **FormData** fields (not URL query params). **`sendEmail=false`** skips the owner notify only.
- **`?dev=true` is removed** — never gate or preserve this (or `testing` / submit-form control flags) on guest or calendar URLs. Legacy links that still include them are stripped on load.

There is **no** separate test-booking mode, `?testing=true`, `is_test_booking` column, or test-data cleanup edge function — use local/staging projects instead.

## 5. Workflow side effects (admin detail page)

Confirm modals on `/bookings/:bookingId` (Proceed, Back, Cancel) **do not** expose per-click side-effect checkboxes. Every transition runs the orchestrator's default happy path; outbound emails are gated by **Property Settings → Email automations** (`propertyAutomationToggles.ts` / `PropertyEmailAutomationsSection`).

Server-side `DevControlFlags` in `_shared/workflowOrchestrator.ts` remain for automated callers (Gmail listener, cron, tests) — the admin UI no longer sends them.

| Email / effect                                                                 | Production control                                              |
| ------------------------------------------------------------------------------ | --------------------------------------------------------------- |
| New booking request (guest submit)                                             | Guest form dev **Send email** (non-prod only; §4)               |
| GAF / pet request PDFs + emails                                                | Property automation toggles + resolved `documentRequirements[]` |
| Booking acknowledgement, parking broadcast, ready-for-check-in, SD refund form | Property automation toggles                                     |
| Manual re-send SD refund form                                                  | **Automation Triggers** on booking detail                       |

## 6. Server contract

- All admin edge functions (`list-bookings`, `transition-booking`, `upload-booking-asset`, `send-sd-refund-form-email`, `parking-broadcast-email`, `cancel-booking`, …) set **`verify_jwt = false`** in `supabase/config.toml` **and** must call `verifyAdminJwt(req)` as the first line of the handler.
  - **Why `false` in config.toml:** Kong's `verify_jwt = true` gate uses the legacy HS256 JWT secret and rejects newer ES256 auth tokens issued by Supabase local dev (≥ 1.x). Our `verifyAdminJwt` calls `supabase.auth.getUser(jwt)` which handles both algorithms correctly **and** also enforces the `ADMIN_ALLOWED_EMAILS` allow-list — making it a strictly stronger gate than Kong's check.
  - **The function body check IS the security boundary** — never skip `verifyAdminJwt(req)` on the assumption that Kong already validated the token.
- The public functions (`submit-form`, `get-form`, `get-booked-dates`, `get-guest-payment-info`, `get-sd-form`, `submit-sd-form`) keep `verify_jwt = false` — guests must still be able to hit them with only the anon key.

## 7. Env vars summary (new)

| Var                                    | Scope | Purpose                                                                                                                                                                                                                                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ADMIN_ALLOWED_EMAILS`                 | edge  | `verifyAdminJwt` allow list + `isPlatformAdmin`; org owners bypass.                                                                                                                                                                                                           |
| `SUPER_ADMIN_EMAILS`                   | edge  | Platform super-admin APIs (`/admin/*`).                                                                                                                                                                                                                                       |
| `VITE_SUPER_ADMIN_EMAILS`              | UI    | Super-admin UX gate (`ModeSwitcher` Admin tab, `RequireSuperAdmin`).                                                                                                                                                                                                          |
| `PARKING_OWNER_EMAILS`                 | edge  | _(Removed — use **`app_settings.parking_owner_emails`** per property.)                                                                                                                                                                                                        |
| `GMAIL_API_WEB_CLIENT_JSON`            | edge  | Web OAuth client JSON for **Connect Google** on **`/settings`** (Gmail `readonly` scope); redirect URI `{SUPABASE_URL}/functions/v1/google-mail-oauth-callback`. Pair with `GMAIL_OAUTH_TOKEN_ENCRYPTION_KEY`.                                                                |
| `GMAIL_OAUTH_TOKEN_ENCRYPTION_KEY`     | edge  | 32-byte AES key (64 hex or base64) for encrypting Gmail refresh tokens + Telegram bot tokens in DB.                                                                                                                                                                           |
| `GMAIL_OAUTH_ALLOWED_RETURN_ORIGINS`   | edge  | Comma-separated SPA origins allowed by `google-mail-oauth-start` (defaults include local Vite).                                                                                                                                                                               |
| `SD_REFUND_CRON_EMAIL_LEAD_MINUTES`    | edge  | _(Removed — use **`app_settings.sd_refund_cron_email_lead_minutes`** per property.)                                                                                                                                                                                           |
| `SD_REFUND_CRON_MAX_CHECKOUT_AGE_DAYS` | edge  | _(Removed — use **`app_settings.sd_refund_cron_max_checkout_age_days`** per property.)                                                                                                                                                                                        |
| `TELEGRAM_*_CRON_SECRET`               | edge  | _(Optional)_ Cron auth headers only (`TELEGRAM_CRON_SECRET`, `TELEGRAM_STAFF_CRON_SECRET`, `TELEGRAM_ADMIN_CRON_SECRET`, `TELEGRAM_FINANCE_CRON_SECRET`, `TELEGRAM_MAINTENANCE_CRON_SECRET`). Bot tokens + chat IDs are **per-property DB** (`telegram_*_settings`), not env. |
| `GEMINI_API_KEYS`                      | edge  | _(Optional)_ Comma-separated Gemini API keys from **different** Google Cloud projects; round-robin with 429 failover. Falls back to `GEMINI_API_KEY` if unset.                                                                                                                |
| `GEMINI_API_KEY`                       | edge  | _(Optional, legacy)_ Single Gemini key. Used only when `GEMINI_API_KEYS` is not set.                                                                                                                                                                                          |
| `GROQ_API_KEY`                         | edge  | _(Optional)_ Groq free-tier key (Llama 4 Scout vision). Fallback when all Gemini keys are rate-limited. Sign up at `https://console.groq.com`.                                                                                                                                |
| `TELEGRAM_FINANCE_CRON_SECRET`         | edge  | _(Optional)_ When set, `telegram-finance-cron` requires header `X-Telegram-Cron-Secret` with the same value.                                                                                                                                                                  |
| `TELEGRAM_MAINTENANCE_CRON_SECRET`     | edge  | _(Optional)_ When set, `telegram-maintenance-cron` requires header `X-Telegram-Cron-Secret` with the same value.                                                                                                                                                              |

Remember to update `docs/architecture/validation-and-env.md` Environment variables in the same commit that introduces any of these.

## 8. Don'ts

- Don't pass JWTs or admin emails through URL query strings.
- Don't add a second Supabase client — share `ui/src/lib/supabase/client.ts`.
- Don't hardcode `kamehome.azurenorth@gmail.com` in code; read from env.
- Don't build admin features that work without `verifyAdminJwt` server-side — the allow list is the one real gate.
