---
title: 'Copy property settings to other properties'
status: for-testing
tags: [workflow, for-testing, org, property, settings, multi-tenancy]
updated: 2026-09-06
stage: for-testing
kind: plan
---

# Copy property settings to other properties

## Goal

Let a host reuse one property's configuration across their other properties without redoing setup unit by unit. From the org **Properties** page a host picks a **source** property, selects which configuration groups to copy (pricing, marketing designs, templates, notification/bot config, guest-form toggles, house rules, amenities, cancellation policy, public-page design, voice/AI settings, custom team roles, recurring finance/maintenance items, …), picks one or more **target** properties, reviews a per-target summary of what will change (including anything skipped because the target's plan or the actor's permissions don't allow it), and confirms. The operation copies **configuration and settings only** — never bookings, guests, finance/maintenance actuals, team members, Meta connections, plan/billing, calendars, reviews, or any other per-listing operational data. A server-side registry defines exactly what each group reads, how identity/data is stripped, and how it is written to each target, so "what is copyable" is one auditable list.

## Review findings (verified against the codebase 2026-09-06)

The first draft was checked against migrations, edge functions, and shared services. Corrections folded into this version:

1. **Reuse existing seeding infra — do not re-implement row creation.** `create-property` already calls `seedPropertySettings()` (`supabase/functions/_shared/propertySettingsSeed.ts`) + `seedPropertyTeamTemplates()` (`_shared/propertyTeamTemplates.ts`). Both are idempotent and already handle the integer-`id` PK quirk on `app_settings` / `telegram_*_settings` (insert, then bump `id` on `23505`). The clone calls `ensurePropertySettings(targetId)` first so every backing row exists, then does **UPDATE**s only — sidestepping the PK/`CHECK (id = 1)` question entirely.
2. **Auth helper is `verifyPropertyAccess(req, propertyId, leaf?)`** from `_shared/orgAuth.ts` → `{ user, property, org, accessKind, permissions }`. It takes an explicit id, so the batch calls it once per (target, leaf). The `resolveScopedPropertyAccess` helper used by `app-settings` / `voice-receptionist-settings` reads a single `?property_id=` query param and is **not** usable for a multi-target batch — the copy function must not route through it.
3. **`app_settings` is one row per property (`app_settings_property_id_unique`) but backs 8+ groups** (pricing, branding/socials, vouchers, email automation, building forms, payment, GAF). Its groups **must** use curated per-group column allowlists. Single-purpose tables (`telegram_*_settings`, `ai_platform_property_settings`, `property_smart_pricing_settings`, `voice_receptionist_settings`, `custom_pages`, `public_page_configs`, `property_template_contents`) can use a **column denylist** (`id`, `property_id`, `organization_id`, `created_at`, `updated_at`, + credentials). This split is now explicit in §2.
4. **Only payment methods are OTP-gated.** `SETTINGS_VERIFICATION_ACTION = 'payment_settings'`; the OTP token binds to a `patchFingerprint` computed from the payment-methods array **and** one property. A batch to N targets needs N fingerprints / N verifications. → **`payment` is out of v1** (its own follow-up). GAF/building-form text + signature and email automations are **not** OTP-gated and are safe to copy.
5. **Drop the `aiAssistant` clone group.** `ai_dashboard_assistant` is org-level only (`ai_dashboard_assistant_org_settings` keyed by `organization_id`; the only per-property surface is a `disabled_property_ids UUID[]` toggle). Nothing per-property to copy.
6. **Voice receptionist config lives in `ai_platform_property_settings.feature_configs.voice_receptionist`** (JSONB), not `voice_receptionist_settings` scalar columns. `voiceReceptionist` and `aiOverrides` groups share `ai_platform_property_settings` — write through the existing service helpers (`_shared/voiceReceptionistService.ts#updateVoiceReceptionistSettings` and the `ai-platform-property-settings` patch path) so a JSONB deep-merge never clobbers the other group.
7. **`custom_pages` / `public_page_configs` `page_type` enums widened over time** (base allowed only `stay_guide`, then `property_landing`, then `property_showcase`). The clone enumerates the source's **actual rows** — never a hardcoded page-type list. Both have `UNIQUE (property_id, page_type)` and are lazily created → clone = upsert on that constraint. Strip `published` from showcase `config`.
8. **`app_settings` has an in-memory TTL cache** (`invalidateAppSettingsCache(propertyId)`); call it for every target after writing. Same discipline for other cached shared readers touched (email branding, entitlements).
9. **No multi-statement transactions from an edge function.** Writes are per-(target, group) and individually atomic at best. The contract is **idempotent, re-runnable, best-effort with a full per-target/per-group result**. Groups that touch several rows atomically (recurring finance/maintenance series, team roles) get an optional `plpgsql` RPC in a later phase for true atomicity.
10. **Telegram tables carry more columns than their base `CREATE`** (later migrations added `notify_on_daily_default`, `daily_reminder_times_manila`, …). The denylist approach is future-proof; still always exclude `bot_token_encrypted`, `chat_id_encrypted`, `google_calendar_id`, `google_spreadsheet_id` unless the credential opt-in is set. The shared bot token is `app_settings.telegram_global_bot_token_encrypted` (AES-256-GCM, shared env key → copying ciphertext works).
11. **Entry-point permission:** there is no `org:properties:configure`. Gate the menu item on the actor having ≥1 target they can edit; real enforcement is the per-(target, group) leaf check in the edge function. No new permission id.
12. **`property_custom_roles`**: `UNIQUE (property_id, lower(trim(name)))`. Seeded templates (Full Access / Operations / Read Only) already exist on every target via `seedPropertyTeamTemplates` → they collide → only genuine custom roles copy. Validate each permission id against the live catalog (`_shared/propertyTeamPermissions.ts`) and drop removed/unknown leaves. Check `20261231140100_org_team_template_role_ids.sql` for role-id shape changes before implementing.

## Scope

### In

- New **Copy settings** entry in the `OrgPropertyActionsMenu` (⋯) on `OrgPropertyCard` / `OrgPropertyListRow`, plus a header action on the org Properties page. Source = the property whose menu was opened (or a picker when launched from the header).
- `CopyPropertySettingsDialog` — 4 steps: **Source** → **What to copy** (grouped checkbox tree) → **Targets** (multi-select of the org's other properties, "Select all") → **Review & confirm** (per-target change/skip summary from a dry run).
- New edge function `copy-property-settings` (POST) driven by `_shared/propertySettingsClone.ts` (the registry).
- `_shared/propertyAssetClone.ts` to duplicate Storage objects (gallery media, GAF signature, template section images) into the target's path and rewrite URLs (Phase 2).
- `property_settings_copy_log` table + in-app notification + email notice to org owner / affected target teams.
- Per-target enforcement: `verifyPropertyAccess` on the source (`settings:view`) and on every target for each selected group's edit leaf; plan-feature gate per group per target — **skip + report**, never hard-fail the batch.
- Phased delivery: Phase 0 foundation → Phase 1 safe groups end-to-end (shippable on its own) → Phase 2 asset groups → Phase 3 recurring items → Phase 4 polish + dry-run diff.

### Out

- **Parking** listings (separate vertical). Registry pattern can extend to parking later.
- Cross-organization copy (targets are always same-org, enforced by comparing `organization_id`).
- Any **operational data** (full SKIP list in §2). Non-negotiable.
- **Plans / subscriptions / entitlements** — billing is org-level with per-property assignment (`org_subscription_properties`); targets keep their own plan and the copy respects it.
- **Team members / invitations** — only custom **role definitions** copy.
- **Announcement read state** — per-org, per-host, device-local (`hostAnnouncementReadState`); explicitly excluded, no change.
- **Payment methods** — deferred (OTP fingerprint is per-property; see finding 4). Own follow-up plan.
- A generic "duplicate this whole property" / new-property-from-template flow.

## Approach

### 1. Copyable-surface registry (the core) — `_shared/propertySettingsClone.ts`

Exports an ordered list of **groups**. Each group:

```ts
type CloneGroup = {
  id: CloneGroupId; // stable key used by UI, log, dry run
  label: string;
  editLeaves: TeamPermissionId[]; // required on every target
  planFeature?: PlanFeatureKey; // gated per target; skip+report when off
  assets?: boolean; // needs propertyAssetClone
  defaultOn: boolean; // UI initial state
  read(sourceCtx): Promise<GroupPayload>;
  sanitize(payload, targetCtx): GroupPayload; // strip identity + data + dead refs; clamp to target limits
  hasNonDefault(targetCtx): Promise<boolean>; // review hint ("already customized")
  write(payload, targetCtx): Promise<void>; // idempotent replace on target
};
```

`sanitize` removes identity. **Always stripped / recomputed on the target**, every group: `name`, `slug`, `type`, `tower`, `unit_number`, `residence_name`, `address`, `settings.{city,province,country,zipCode,latitude,longitude,mapsUrl,placeId}`, `app_settings.gaf_tower_and_unit_number` (recomputed from the target's tower+unit), `public_guest_app_origin`, every `id`/`property_id`/`organization_id`, storage paths (re-derived), OTA/Meta identifiers.

**Write model:**

- `app_settings` groups → curated column allowlist, one `UPDATE ... WHERE property_id = $target` after `ensurePropertySettings(target)`; then `invalidateAppSettingsCache(target)`.
- Dedicated-table groups → column denylist copy via the table's existing service helper where one exists, else a scoped `UPDATE`.
- Multi-row groups (templates, telegram-per-module, custom roles, recurring series) → delete-and-reinsert or upsert on the natural unique key, isolated per row.
- Every group write wrapped so one (target, group) failure is recorded and the batch continues.

### 2. Full configuration inventory

Legend: **COPY** (config, in a group) · **SKIP** (operational data, never copied) · **SHARED** (already org-scoped — no-op, not offered) · **COND** (conditional / opt-in) · **DEFER** (post-v1).

| Area (route)                                                                                                               | Storage                                                            | Decision                   | Group / notes                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Basic info — identity (name, slug, type, tower, unit, residence, address)                                                  | `properties.*`                                                     | SKIP                       | Identity.                                                                                                                                                                                                                                                                                                                                                                        |
| Basic info — contact (contactName/Role/Phone/Email)                                                                        | `properties.settings.*`                                            | COPY (opt-in, default off) | `contact`.                                                                                                                                                                                                                                                                                                                                                                       |
| Brand color                                                                                                                | `app_settings.brand_color`                                         | COPY                       | `branding`.                                                                                                                                                                                                                                                                                                                                                                      |
| Property details (bedrooms, bathrooms, floors, unitTypeId, maxAdults/Children, checkIn/Out, selfCheckIn)                   | `properties.settings.*` + `max_guests`                             | COPY                       | `propertyDetails` — `sanitize` validates against the **target's** residence/unit-type via `_shared/propertySettingsValidation.ts` + `_shared/propertyResidenceDefaults.ts`; out-of-range clamped + reported; warn when target residence ≠ source residence.                                                                                                                      |
| Photos & Videos                                                                                                            | `properties.settings.media[]` + Storage `property-media`           | COPY (assets)              | `media` — Phase 2. Objects cloned into target path, URLs + `storagePath` rewritten, primary/order kept.                                                                                                                                                                                                                                                                          |
| Description                                                                                                                | `properties.settings.description`                                  | COPY                       | `listingContent`.                                                                                                                                                                                                                                                                                                                                                                |
| Amenities (`enabledAmenities`, `customAmenities`)                                                                          | `properties.settings.*`                                            | COPY                       | `amenities`.                                                                                                                                                                                                                                                                                                                                                                     |
| House Rules (`enabledHouseRules`, `customHouseRules`)                                                                      | `properties.settings.*`                                            | COPY                       | `houseRules`.                                                                                                                                                                                                                                                                                                                                                                    |
| Cancellation policy                                                                                                        | `properties.settings.cancellationPolicy`                           | COPY                       | `cancellationPolicy`.                                                                                                                                                                                                                                                                                                                                                            |
| Guest Form toggles (allowPets/Parking/SurpriseDecor, complimentaryOwnerParking, cleaningBufferMinutes)                     | `properties.settings.*`                                            | COPY                       | `guestForm`. `preferredOwnerParkingId` **stripped** unless the target has that same ACTIVE org parking; else healed to target default.                                                                                                                                                                                                                                           |
| Socials (facebook/airbnb/instagram/tiktok URLs)                                                                            | `app_settings.*`                                                   | COPY                       | `branding`.                                                                                                                                                                                                                                                                                                                                                                      |
| Payment methods (provider, account name/number, QR)                                                                        | `app_settings.payment_methods[]` + legacy `gcash_*` + Storage      | DEFER                      | OTP fingerprint is per-property (finding 4). Own follow-up.                                                                                                                                                                                                                                                                                                                      |
| Building forms (gaf_unit_owner, onsite contact, owner phone, signature)                                                    | `app_settings.gaf_*` + Storage signature (`app-settings-assets`)   | COPY (assets)              | `buildingForms` — text Phase 1, signature clone Phase 2. Not OTP-gated. `gaf_tower_and_unit_number` recomputed.                                                                                                                                                                                                                                                                  |
| Email automations (recipients, `sd_refund_cron_*`, `automation_toggles`)                                                   | `app_settings.*`                                                   | COPY                       | `emailAutomations`. Timing + toggles by default; recipient addresses are an opt-in inside the group.                                                                                                                                                                                                                                                                             |
| Pricing — rates & fees (weekday/weekend rate, down payment, SD, pet fee, extra-guest fee)                                  | `app_settings.*`                                                   | COPY                       | `pricingRates` — leaf `pricing.rates:edit`.                                                                                                                                                                                                                                                                                                                                      |
| Pricing — holiday rules                                                                                                    | `app_settings.pricing_holiday_rules[]`                             | COPY                       | `pricingRates`.                                                                                                                                                                                                                                                                                                                                                                  |
| Pricing — parking rate                                                                                                     | `app_settings.default_parking_rate_guest`                          | COPY                       | `pricingRates`.                                                                                                                                                                                                                                                                                                                                                                  |
| Smart Pricing settings (strength, min/max, mode)                                                                           | `property_smart_pricing_settings`                                  | COPY                       | `smartPricing` — plan `smartPricing`; recs/runs NOT copied.                                                                                                                                                                                                                                                                                                                      |
| Pricing — date overrides / blocked dates / synced blocks                                                                   | `property_pricing_date_overrides`, `property_blocked_dates`        | SKIP                       | Per-date data.                                                                                                                                                                                                                                                                                                                                                                   |
| Channel Sync feeds / export token                                                                                          | `property_calendar_feeds`, `property_calendar_export`              | SKIP                       | Per-listing external credentials.                                                                                                                                                                                                                                                                                                                                                |
| Templates — standard + email bodies + section images                                                                       | `property_template_contents`                                       | COPY (assets)              | `templates` — leaves `templates.standard:edit` / `templates.email:edit`; email bodies need `customTemplates` on target. Section images cloned Phase 2; text-only works Phase 1.                                                                                                                                                                                                  |
| Templates — custom                                                                                                         | `property_template_contents` (category `custom`)                   | COPY                       | `templates` — needs `customTemplates`; cap 20/property.                                                                                                                                                                                                                                                                                                                          |
| Public Pages — template choice (`template_key` per page_type)                                                              | `custom_pages`                                                     | COPY                       | `publicPages` — leaves `publicPages.*.edit`. Enumerate source rows dynamically.                                                                                                                                                                                                                                                                                                  |
| Public Pages — section visibility/order/style; showcase `published`                                                        | `public_page_configs`                                              | COPY                       | `publicPages` — `published` **not** copied (stays off); live showcase needs `propertyShowcase`.                                                                                                                                                                                                                                                                                  |
| Marketing — custom / AI templates (`designJson`)                                                                           | `marketing_templates`                                              | COND                       | `marketingTemplates` — Phase 2. New ids; embedded gallery URLs remapped by index when `media` is in the same run, else per-target warning. Needs `marketingStudio` + `customTemplates`.                                                                                                                                                                                          |
| Marketing — publications history                                                                                           | `marketing_publications`                                           | SKIP                       | Operational.                                                                                                                                                                                                                                                                                                                                                                     |
| Inbox — pinned snippets                                                                                                    | `properties.settings.inboxPinnedSnippets[]` (max 5)                | COPY                       | `inboxSnippets` — leaf `inbox.quickReplies:edit`.                                                                                                                                                                                                                                                                                                                                |
| Inbox — quick replies / AI automation (tone & rules, auto-reply)                                                           | org-scoped tables                                                  | SHARED                     | Already shared across the org's properties; not offered.                                                                                                                                                                                                                                                                                                                         |
| Inbox — Meta connections / tokens / synced threads                                                                         | `social_channel_connections`, conversations                        | SKIP                       | Per-listing OAuth + messages.                                                                                                                                                                                                                                                                                                                                                    |
| Notifications — Telegram per module (enabled, toggles, thresholds, schedule times, all templates) ×6                       | `telegram_*_settings`                                              | COPY                       | `telegramNotifications` — leaves `notifications.<module>:edit`; plan `telegramNotifications`. Denylist copy.                                                                                                                                                                                                                                                                     |
| Notifications — shared bot token                                                                                           | `app_settings.telegram_global_bot_token_encrypted`                 | COPY                       | Part of `telegramNotifications`.                                                                                                                                                                                                                                                                                                                                                 |
| Notifications — per-module bot token / group chat ID                                                                       | `telegram_*_settings.bot_token_encrypted` / `chat_id_encrypted`    | COND (opt-in, default off) | Sub-choice with warning: targets post to the **same Telegram groups**.                                                                                                                                                                                                                                                                                                           |
| Notifications — Google calendar / spreadsheet IDs                                                                          | `telegram_*_settings.google_*`                                     | SKIP                       | Per-property external resource.                                                                                                                                                                                                                                                                                                                                                  |
| Notifications — in-app feed / OS-push subscriptions                                                                        | notifications, push tables                                         | SKIP                       | Per-user / per-device.                                                                                                                                                                                                                                                                                                                                                           |
| Announcements — read state                                                                                                 | `localStorage` per org                                             | SKIP                       | By design.                                                                                                                                                                                                                                                                                                                                                                       |
| Reviews & vouchers — external reviews                                                                                      | `app_settings.external_reviews[]` + Storage proof                  | SKIP                       | Property-specific moderated proof.                                                                                                                                                                                                                                                                                                                                               |
| Reviews & vouchers — next-stay voucher config (`vouchers_enabled`, `voucher_prizes`, `voucher_reveal_style`)               | `app_settings.*`                                                   | COPY                       | `voucherConfig`.                                                                                                                                                                                                                                                                                                                                                                 |
| Superhost status / proof                                                                                                   | `app_settings.superhost_*`                                         | SKIP                       | Earned org-wide automatically.                                                                                                                                                                                                                                                                                                                                                   |
| Voice Receptionist (enabled, voice_id, persona_prompt, caps)                                                               | `ai_platform_property_settings.feature_configs.voice_receptionist` | COPY                       | `voiceReceptionist` — leaf `settings.voiceReceptionist:edit`; enable needs `aiReceptionist`. Write via `voiceReceptionistService`.                                                                                                                                                                                                                                               |
| AI overrides (enabled, daily/monthly call limit, daily cost limit)                                                         | `ai_platform_property_settings.*` (scalars)                        | COPY                       | `aiOverrides` — leaf `settings.aiOverrides:edit`; hidden unless target plan `aiMonthlyCreditAllowance > 0`. Usage counters NOT copied.                                                                                                                                                                                                                                           |
| Dashboard AI assistant                                                                                                     | `ai_dashboard_assistant_org_settings` (org-level)                  | SHARED                     | No per-property config. Group dropped.                                                                                                                                                                                                                                                                                                                                           |
| Team — custom role definitions                                                                                             | `property_custom_roles` (name + permissions JSON)                  | COPY                       | `teamRoles` — leaf `team.customRoles:add`; needs `customRoles`. Name collision → skip + report; validate permission ids against live catalog.                                                                                                                                                                                                                                    |
| Team — members / invitations / seat assignments                                                                            | `property_members`, `property_invitations`                         | SKIP                       | People + access.                                                                                                                                                                                                                                                                                                                                                                 |
| Finance — recurring series definition (kind, label, amount, category, interval, Telegram reminder config)                  | `finance_line_items` rows sharing `recurrence_series_id`           | COND                       | `financeRecurring` — Phase 3. Read the series' canonical attributes from its latest row; create a **new** series on the target via the same `_shared/financeService.ts` path a manual add uses, anchored at the next future due date. No `paid_at`, no historical `occurred_on`, no one-offs, no booking rows. Dedupe on label+interval+amount. Leaf `finance.transactions:add`. |
| Finance — transactions / ledger / summaries / exports                                                                      | `finance_line_items` (one-off) + bookings                          | SKIP                       | Money data.                                                                                                                                                                                                                                                                                                                                                                      |
| Maintenance — recurring reminder definition                                                                                | `maintenance_items` rows sharing `recurrence_series_id`            | COND                       | `maintenanceRecurring` — Phase 3. Same shape (`daily/weekly/monthly/quarterly/yearly`, `scheduled_on`, telegram reminder fields). New series anchored at next due date. Leaf `maintenance.reminders:add`.                                                                                                                                                                        |
| Maintenance — one-off reminders / done history                                                                             | `maintenance_items` (one-off)                                      | SKIP                       | Task data.                                                                                                                                                                                                                                                                                                                                                                       |
| Bookings / guest submissions / calendar stays                                                                              | `guest_submissions`, bookings                                      | SKIP                       | Core data.                                                                                                                                                                                                                                                                                                                                                                       |
| Guest reviews (Kame) / guest portal chat / web chat                                                                        | `guest_reviews`, conversations                                     | SKIP                       | Guest data.                                                                                                                                                                                                                                                                                                                                                                      |
| Org settings (email routing, email logo, facebook URL, org brand color, verification, developments, document requirements) | `org_settings`, `organizations.settings`, developments             | SHARED                     | Org / development level; not offered.                                                                                                                                                                                                                                                                                                                                            |

### 3. Dialog UX — `CopyPropertySettingsDialog`

- `ResponsiveModal` / `AdminDialogShell`, 4 steps with a segmented progress header (same pattern as the Marketing "Generate with AI" wizard).
- **Step 1 — Source:** pre-filled from the card ⋯ menu; a `Select` of the org's ACTIVE + INACTIVE properties from the header.
- **Step 2 — What to copy:** grouped checkbox tree (Listing content · Pricing · Guest form · Notifications & bot · Templates · Public pages · Marketing · AI & voice · Team roles · Recurring finance · Recurring maintenance). Parent = select all leaves; each leaf shows its plan badge. `contact`, per-module Telegram credentials, and email recipient addresses default **off**.
- **Step 3 — Targets:** checkbox list of every **other** property in the org (name + tower/unit + plan pill + status), "Select all", search. Rows the actor can't edit are disabled with a reason tooltip.
- **Step 4 — Review:** calls `copy-property-settings` with `dryRun: true`; renders per target — `N groups apply`, `M skipped (plan)`, `K skipped (permission)`, `already customized: …`. Primary **Copy settings** runs it for real. Success toast (`Settings copied to N properties`); partial failures list target/group + server reason.
- Mobile: full-screen sheet, one step per screen, 44px targets.

### 4. Edge function — `copy-property-settings`

- `serveAuthenticated('copy-property-settings', handler)`, POST only; registered in `supabase/config.toml` with the same `verify_jwt` policy as other authenticated functions.
- Body: `{ sourcePropertyId, targetPropertyIds: string[], groups: CloneGroupId[], options: { copyContact, copyEmailRecipients, copyTelegramCredentials }, dryRun?: boolean }`.
- `verifyPropertyAccess(req, sourcePropertyId, 'settings:view')`; load source `organization_id`; reject any target whose `organization_id` differs.
- Per target: `ensurePropertySettings(targetId)`; then per selected group — for each `editLeaves` leaf `verifyPropertyAccess(req, targetId, leaf)` (catch → `skipped: { group, reason: 'permission' }`); `resolvePropertyEntitlements(targetId)` and check `planFeature` (→ `skipped: { group, reason: 'plan' }`).
- `read` once per group from the source; then `sanitize` + `write` per target inside a per-(target, group) try/catch. `dryRun` runs `read` + `sanitize` + `hasNonDefault` only, no `write`.
- Asset groups call `propertyAssetClone` before `write`.
- On a real run: insert one `property_settings_copy_log` row (`results` JSONB per target/group), fire `createNotification` per affected target + one dedup email to the org owner and target teams (reuse the post-payment-save notice sender pattern).
- Response: `{ dryRun, results: [{ targetPropertyId, applied: CloneGroupId[], skipped: [{ group, reason }], failed: [{ group, error }], alreadyCustomized: CloneGroupId[] }], logId? }`.

### 5. Asset cloning — `_shared/propertyAssetClone.ts`

- Input: source object URLs/paths + target `property_id` + bucket id.
- Downloads each object with the service client, re-uploads to the target's path using `_shared/uploadService.ts` conventions, returns `Map<oldUrl, newUrl>` for the group's `sanitize` to rewrite.
- Buckets: `property-media` (gallery), `app-settings-assets` (GAF signature), template section-image bucket (confirm exact id — `templates` vs `property-templates` — in Phase 2), `marketing` (design assets).
- Per-object failures reported, never fatal; never deletes source objects.

### 6. Guardrails

- **Idempotent replace** per group — safe to re-run; a second identical copy is a no-op.
- **Batch isolation** — one (target, group) failure never affects another.
- **Plan-aware** — a group the target's plan disallows is skipped + reported, never forced.
- **No privilege escalation** — every write passes the same leaf + plan gate a manual edit would.
- **Cache coherence** — `invalidateAppSettingsCache(target)` + any other shared-reader cache after each write.
- **Audit + notify** — `property_settings_copy_log` + in-app + email.
- **Registry = source of truth** — mirrored in `docs/PROJECT.md`; a new settings surface later means a new registry entry (add to the `documentation-maintenance` checklist).

## Implementation tasks

### Phase 0 — foundation

- [x] `supabase/migrations/<ts>_property_settings_copy_log.sql` — `property_settings_copy_log` (`id`, `organization_id`, `source_property_id`, `actor_user_id`, `groups TEXT[]`, `target_property_ids UUID[]`, `results JSONB`, `created_at`). RLS on, service_role only. Add to `docs/archive/operations/migration-runbook.md`.
- [x] `supabase/functions/_shared/propertySettingsCloneTypes.ts` — `CloneGroupId` union, `CloneGroup`, payload + result types.
- [x] `supabase/functions/_shared/propertySettingsClone.ts` — registry skeleton, `IDENTITY_STRIP_KEYS`, `sanitizeIdentity()`, `runCloneGroup()` wrapper (try/catch + result shape). No groups yet.
- [x] `ui/src/features/dashboard/org/lib/copyPropertySettingsGroups.ts` — UI catalog (id, label, plan badge, `defaultOn`) mirrored from the edge list; unit test asserting id parity between the two files.

### Phase 1 — safe (no-asset, non-OTP) groups + full flow (shippable)

- [x] Implement groups: `propertyDetails`, `listingContent`, `amenities`, `houseRules`, `cancellationPolicy`, `guestForm`, `branding`, `contact` (opt-in), `emailAutomations`, `pricingRates`, `smartPricing`, `voucherConfig`, `publicPages`, `templates` (**text only** initially; upgraded with assets in Phase 2), `telegramNotifications` (denylist + shared bot token; credentials opt-in), `inboxSnippets`, `voiceReceptionist`, `aiOverrides`, `teamRoles`.
  - Reuse: `_shared/appSettings.ts`, `_shared/propertySettingsValidation.ts`, `_shared/propertyResidenceDefaults.ts`, `_shared/propertyAutomationToggles.ts`, `_shared/customPages.ts`, `_shared/publicPageConfigs.ts`, `_shared/propertyTemplates.ts`, `_shared/propertyTeamPermissions.ts`, `_shared/voiceReceptionistService.ts`, telegram settings shared modules, `_shared/propertySettingsSeed.ts`, `_shared/planEntitlements.ts`.
- [x] `supabase/functions/copy-property-settings/index.ts` — orchestrator (§4), incl. `dryRun`; minus asset/recurring branches (those land in Phase 2–3).
- [x] `supabase/config.toml` — register the function.
- [x] `_shared/propertySettingsClone.ts` — `hasNonDefault` per group.
- [x] Notifications: `property_settings_copied` type in `_shared/notificationService.ts` + renderer in `ui/src/features/dashboard/notifications`; dedup email via the existing post-save notice sender. Migration `20261306120100_property_settings_copied_notification_type.sql`.
- [x] UI:
  - [x] `ui/src/features/dashboard/org/components/org-properties/CopyPropertySettingsDialog.tsx`.
  - [x] `ui/src/features/dashboard/org/hooks/useCopyPropertySettings.ts` — TanStack mutation; on success invalidate `properties`, `app-settings`, templates, telegram, pricing, entitlements query keys for each target.
  - [x] `ui/src/features/dashboard/org/lib/copyPropertySettingsApi.ts`.
  - [x] Wire **Copy settings** into `OrgPropertyActionsMenu` (`OrgPropertyCard.tsx`) + a header action in `OrgPropertiesPage.tsx`; shown when org has ≥2 properties.
  - [x] Review-step result view (applied / skipped-plan / skipped-permission / failed / already-customized).
- [x] Tests: Deno tests for `sanitizeIdentity` + `runCloneGroup` + representative groups (`propertySettingsClone_test.ts`); Vitest catalog id parity (`copyPropertySettingsGroups.test.ts`). _(RTL wizard + Playwright E2E not added.)_
- [x] `bun run type-check && bun run lint` (module files clean; repo has 1 pre-existing unrelated lint error in ParkingSettingsCard) && Vitest catalog parity + Deno clone tests. Playwright E2E not added.

### Phase 2 — asset-bearing groups

- [x] `_shared/propertyAssetClone.ts` (§5). _(Dedicated fake-client Deno tests not added; used by Phase 2 groups.)_
- [x] `media` group — clone `property-media`, rewrite `settings.media[].url` + `storagePath`, keep primary + order.
- [x] `buildingForms` — clone GAF signature object; recompute `gaf_tower_and_unit_number`.
- [x] Upgrade `templates` to clone `section_image_url` objects.
- [x] `marketingTemplates` group — copy `marketing_templates` rows with new ids; remap embedded gallery URLs when `media` is in the same run, else per-target warning; gate `marketingStudio` + `customTemplates`.
- [x] UI: Phase 2 groups in wizard (`copyPropertySettingsGroupsForPhase(3)`). _(Dedicated "marketing designs may reference source photos" inline warning not added.)_

### Phase 3 — recurring finance + maintenance

- [x] Recurring clone via Phase 3 groups in `_shared/propertySettingsClonePhase23.ts` (read series canonical attributes; create fresh series on target anchored at next future due date; exclude history / `paid_at` / one-offs; dedupe on label+interval+amount).
- [x] `financeRecurring` group (leaf `finance.transactions:add`) + `maintenanceRecurring` group (leaf `maintenance.reminders:add`).
- [x] Deno coverage via clone registry tests / group registration.
- [x] UI leaves (default off) with phase-3 visibility in the wizard.

### Phase 4 — polish

- [x] Optional `plpgsql` RPCs for atomic multi-row groups (team roles, recurring series). **Deferred / skipped for v1.**
- [x] "Copy from…" affordance on the target property's own Settings header (inverse direction), same dialog (`?copyTarget=` + locked target step).
- [x] Per-group "only where the target is still default" toggle (`skipAlreadyCustomized`).
- [x] `property_settings_copy_log` viewer (last runs) — org Properties **Copy history** via `action: 'listLogs'`.
- [x] Confirm step when targets > 10 (extra checkbox before copy).

## Docs to update

- [x] `docs/PROJECT.md` — `copy-property-settings` function, `property_settings_copy_log` table, registry summary (link here for the full table).
- [x] `docs/guides/routes/org/properties.md` — the **Copy settings** entry point, wizard, what is / isn't copied, per-target plan/permission skips (invoke `route-guides`).
- [x] `docs/guides/routes/org/property/settings.md` — note values can be seeded from another property; **Copy from…** on Settings header.
- [x] `docs/guides/routes/org/property/{pricing,templates,notifications,public-pages,marketing,team,finance,maintenance}.md` — one line each: bulk-copyable via org Properties → Copy settings.
- [x] `.cursor/rules/supabase-edge-functions.mdc` — add `copy-property-settings` + JWT policy row.
- [x] `.cursor/rules/admin-auth.mdc` — new endpoint + the per-target leaf-check pattern.
- [x] `docs/archive/operations/migration-runbook.md` — `property_settings_copy_log` + `property_settings_copied` notification type.
- [x] `docs/architecture/edge-functions.md` + `docs/architecture/data-model.md` — API row + shipped table note.
- [x] `plans-and-permissions` skill checklist — record the per-group plan features + reused `settings.*` / module edit leaves (no new plan feature, no new permission id). Documented N/A in properties route guide.
- [x] `docs/workflow/planned/README.md` — index row (added with this plan).

## Open questions

- Marketing designs embedding source gallery URLs — MVP is "remap by index when Photos are also copied, else warn". Accept, or make `media` a hard prerequisite for `marketingTemplates`?
- Payment methods copy — build a dedicated `copy_payment_settings` verification action later, or leave payment permanently manual per property?
- `financeRecurring` / `maintenanceRecurring` in v1 scope creep — keep as Phase 3, or split into a separate plan once Phases 1–2 ship?
