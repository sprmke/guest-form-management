---
title: 'Mobile native redesign'
stage: in-progress
status: in-progress
updated: 2026-09-08
---

# Ground-Up Mobile Redesign — "Feels Like a Native App"

## Implementation progress (2026-08-04)

| Phase                                           | Status                  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **0 — Foundation**                              | Done (dashboard-scoped) | `ui/src/components/mobile/*`, `useIsMobile`, sheet/bottom-sheet/`ResponsiveModal`, `AdminCardGrid`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **3 — Dashboard admin shell**                   | Done                    | `AdminLayout` bottom tabs + More sheet; `BookingEditStickyBar` → `ContextualActionBar`; desktop sidebar untouched                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **4 — Remaining dashboard modules**             | Done (dashboard)        | Super-admin mobile grid + Approvals cards; ~36 Dialog → `ResponsiveModal`. Team permissions matrix deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **4b — Native content polish**                  | Done (dashboard)        | Floating pill tab bar; `shadow-native` / `native-cta` / `native-page-canvas` / stagger; Finance + Dashboard + Bookings cards                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **4c — Bookings alignment pass**                | Done                    | Single-col phone list cards; toolbar equal-width filters; full-bleed view toggle; Finance toolbar matched                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **4d — Floating panel polish**                  | Done (dashboard)        | `FloatingPanel` / `FloatingToolbar` / `ListSheet` / `MobilePageStack`; wired on Bookings, Finance, Property Dashboard. **Desktop isolation:** `shadow-card` restored; float/canvas/stagger scoped `max-lg` only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **4e — Brand hero reskin**                      | Done (Phase A)          | `MobileBrandHero` / `MobileHeroOverlap` / `AdminMobilePage`; Dashboard, Bookings, Finance reskinned; layout frosted header hidden when page owns hero; on-primary tenant switcher. Desktop `lg+` unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **4f — Hero icon actions + rollout**            | Done (dashboard)        | `MobileHeroActionButton` beside tenant switcher; icon-only primary CTAs. Shell on Maintenance, Marketing, Pricing, Team, Inbox, Notifications, Templates, **Settings** (property/org/parking). Horizontal section chip strip removed on `max-lg`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **4h — Tab-bar content clearance**              | Done (dashboard)        | Shell `p-*` was twMerging away `bottomTabBarOffsetClassName()`; fixed via `px`/`pt` + `max-lg` pb (`7.25rem` + safe area) on `MobileAppShell` so floating tabs no longer cover page bottoms (e.g. Settings Danger Zone).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **4i — Org + parking shell rollout**            | Done (dashboard)        | Org dashboard / properties / parkings + parking dashboard, bookings, finance, pricing, notifications, booking detail on `AdminMobilePage`. Matches property brand-hero shell; desktop `lg+` unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **4j — Single hero action menu**                | Done (dashboard)        | `MobileHeroActionMenu`: one hero button max (1 action = direct icon; 2+ = ··· dropdown). Inbox Channels / Quick replies / Automation consolidated on hero + desktop Manage.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **4k — Bottom tab bar polish**                  | Done (dashboard)        | Solid brand active pill; icons ~18px / stroke 1.75 (not chunky 22px); 10px labels; 48px row; shared glass dock; content clearance `7.75rem`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **4l — Compact list refine chrome**             | Done (dashboard)        | Mobile list toolbars (Bookings, Finance, Maintenance): one search row + refine sheet (filters/sort/per-page) + view toggle. Shared `AdminListRefineSheet`. Desktop `lg+` inline toolbars unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **4m — Mobile density distill**                 | Done (dashboard)        | max-lg: hide hero subtitles, KPI icons + “vs last period”, chart _descriptions_ (icons kept compact via **4r**), period eyebrows, cash-flow legend; compact attention chips; flat occupancy KPIs (ring retired); denser org list rows; `dense` on org/property/parking dashboards. Desktop `lg+` unchanged.                                                                                                                                                                                                                                                                                                                                                                                                         |
| **4n — Native choice sheets**                   | Done (dashboard)        | Tiny floating dropdowns → `MobileChoiceSheet` on `max-lg`: date “View by”, Guest pages, hero action menus, sort/select filters, Finance/Maintenance hero export menus. Shared `MobileChoiceSheet` / `MobileChoiceItem`. Desktop popovers/menus unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **4o — Fill-main scroll under hero**            | Done (dashboard)        | `AdminMobilePage` is a `flex-1 min-h-0` column so Templates/Settings/Notifications keep an inner scrollport when `useAdminLayoutFillMain` locks outer scroll.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **4p — Sticky collapsing brand hero**           | Done (dashboard)        | On scroll (`max-lg`), brand hero pins on **all** `AdminMobilePage` routes (fill-main + body scrollport — not only Settings). Title/arc parallax collapse; switcher + actions stay. `PageTransition` clears leftover transform; fillMain is ref-counted.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **4q — Comfortable mobile rhythm**              | Done (dashboard)        | Roomier section/card gaps + padding (`MobilePageStack`, FloatingPanel, StatCard, booking cards, charts). KPI labels `text-xs`, values `text-lg`. Ops-dense, not sparse. Desktop mostly unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **4r — Card title optical alignment**           | Done (dashboard)        | Shared `AdminSurfaceCardHeader`: compact icon well + title left / action right on **one row** at every breakpoint (no mobile stack under title). Card-header segments use dense equal-width pills (`cardHeaderSegmented*ClassName`). Subtitle still desktop-only.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **4s — Sheet split scroll**                     | Done (dashboard)        | Bottom sheets use `h-[min(92dvh,max-content)]` so tall content gets a definite height + working inner scroll (Get Verified no longer clips). Sticky header/footer → `sheetLayout="split"`. Simple forms keep default `scroll`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **4t — Sheet frosted backdrop**                 | Done (dashboard)        | Sheet overlay: `bg-black/40 backdrop-blur-sm` (was solid `bg-black/80`), closer to desktop Dialog (`bg-black/50 backdrop-blur-sm`). `overlayClassName` passes through Sheet → ResponsiveModal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **4u — More sheet full nav**                    | Done (dashboard)        | More sheet lists **all** navigable pages (incl. primary dock tabs: Dashboard/Bookings/Inbox…), not only overflow. Primary dock: Dashboard, Bookings, Inbox, Notifications, Assistant, More (Finance under More).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **4v — Float scrolls (not sticky)**             | Done (dashboard)        | Hero overlap toolbar stays floated (tuck + shadow) but lives in the scrollport — scrolls away with content; only brand hero stays pinned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **4w — Hero arc seam**                          | Done (dashboard)        | Removed canvas `rounded-t` that fought the hero ellipse (Calendar etc. looked double-humped). Single arc from `.mobile-brand-hero` only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **4x — Full-bleed canvas restore**              | Done (dashboard)        | Fixed blue side gutters: white canvas full-bleed under hero; float inset only. Shell transparent when hero-owned. Stronger hero arc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **4y — Drop sticky, keep parallax**             | Done (dashboard)        | Reverted sticky hero + fillMain nested scrollport (layout was broken). Simple `main` scroll; title/arc parallax kept via scroll progress.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **4z — Sticky chrome morph**                    | Done (dashboard)        | Safe reintro: float/hero sentinel → fixed brand-teal `MobileStickyChrome` + compact floating toolbar pill. Bookings/Parking: date sticky + More for filters. Document scroll only; no nested scrollport.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **4aa — Fill-main scroll restore**              | Done (dashboard)        | Restored `AdminMobilePage` flex `min-h-0 flex-1` chain so Settings/Notifications/Templates/Inbox inner scroll works under the hero. Sticky pin skipped when sentinel is outside the scrollport.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **4ab — No-float cutoff fix**                   | Done (dashboard)        | Fill-main skips hero `-mt-6` tuck. Shell tab `pb` disabled on fill-main (was a dead white gap that clipped mid-card); tab clearance lives on the section/inbox scrollport instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **4g — Switcher & stat-ring polish**            | Done (dashboard)        | Org/property/parking switcher opens a `BottomSheet` list on `max-lg`. Occupancy ring was later retired in **4m** in favor of flat KPI cards on all breakpoints.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 1a — Guest operational shell (`MainLayout`)     | Planned                 | `BottomBarSlotProvider` + `MobileAppShell` on `MainLayout.tsx`; floating corner buttons (admin entry/theme toggle) normalized into a slim top bar; tab bar on `calendar`/`messages`/`document`. `stay-guide` and `showcase` are excluded — already responsive/intact, out of scope. See detailed breakdown below.                                                                                                                                                                                                                                                                                                                                                                                                   |
| 1b — Guest form wizard → `ContextualActionBar`  | Planned                 | `GuestFormStepNavigation.tsx` promoted to `ContextualActionBar` for `form`/`sd-form`/`guest-review`/`parking/:bookingId`; `success` returns to tab bar; exit affordance while tab bar hidden. Highest-risk file: `GuestForm.tsx` (2,696 lines) — ship + verify alone first.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2a — Marketing shell (`MarketingLayoutShell`)   | Planned                 | Bottom tabs (Explore/Properties/Parkings+Developments/Account) + "More" sheet, retiring the hamburger `AnimatePresence` overlay. Single change covers landing/search/for-hosts/pricing/services/host-public/properties/parkings/developments/about/contact/support/legal.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2b — PDP CTAs → `ContextualActionBar`           | Planned                 | `PropertyDetailPage`/`ParkingDetailPage`/`DevelopmentDetailPage`/`HostPublicPage` primary CTA becomes a `ContextualActionBar` consumer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2c — Guest portal shell migration               | Planned                 | `GuestAccountLayout`/`GuestAccountMobileNav` top `sticky top-16` strip → shared `BottomTabBar`/`BottomBarSlot` (top→bottom placement change, call out explicitly). Reuses `SlidingActivePill`/`useSlidingActivePill`. Covers `/account/profile`,`/stays`,`/vouchers`,`/favorites`,`/tickets/*`.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2d — Auth pages decision                        | Planned                 | `AuthLayout` (sign-in, for-hosts/for-guests login/register): default no tab bar (linear trust flow); bring up to touch-target/spacing/type-scale rules only. Confirm default holds during implementation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2e — Guest-facing modal sweep                   | Planned                 | `ContactHostSheet` → real `ResponsiveModal`/sheet primitive; `PropertyRules`/`PropertyAmenities`/`BookingCalendarModal` → sheet; `GuestAuthModal` stays centered; media/document lightboxes stay full-bleed special cases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2f — Property-scoped read screens polish        | Planned                 | `document`/`messages`: inherit 1a shell already; this pass is touch-target/spacing/type-scale conformance only, no new shell work. `stay-guide` and `showcase` are excluded from this whole module — already responsive/intact, do not modify.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Closing audit — dashboard gaps + full sweep     | Planned                 | Close out or explicitly re-defer: Team permissions matrix mobile card view (Phase 4), Phase 5 doc updates (partial). Full Playwright viewport sweep (375/390/768/1024/1440 + dark + reduced-motion) across every route in the public inventory, including ones not directly touched above (`NotFoundPage`, legal pages, `ParkingRequestStatusPage`).                                                                                                                                                                                                                                                                                                                                                                |
| **4ac — Editor surfaces full-bleed**            | Done + verified         | Marketing Studio (Calendar / Polotno Design / Remotion Video) + Public Pages editor: `max-lg` full-bleed canvas, sidebar → `MarketingEditorMobilePanel` bottom sheet (template select auto-closes it), `MarketingEditorMobileToolbar` as its own floating pill **above** the app tab bar (tab bar stays visible for nav), `useAdminLayoutFillMain`. Studio card `max-lg:h-[calc(100dvh-20.5rem)] max-lg:flex-none` reserves both stacked docks. Calendar month nav is a full-width top bar on `max-lg`. Playwright-verified at 375 / 390 / 768 / 1024 / 1440 + Publish flow. See [`../planned/marketing-studio-mobile-and-dashboard-responsive.md`](../planned/marketing-studio-mobile-and-dashboard-responsive.md) |
| **4ad — Pricing calendar touch**                | Done + verified         | `PricingCalendarGrid` two-tap range select on `max-lg` (pointer drag doesn't exist on touch); no tip banner (layout shift); legend `lg+` only; border-only selection highlight on phone; shorter day cells `<sm`; shared parking + property. Verified 375 / 390 (range select end-to-end → date modal).                                                                                                                                                                                                                                                                                                                                                                                                             |
| **4ae — Dashboard dialog/toolbar polish**       | Done + verified         | `AdminParkingNewBookingModal` + `PricingSaveDialog` → `ResponsiveModal`; `OrgProperties/ParkingsToolbar` Status+Type → `AdminListRefineSheet` on `max-lg`. Refine sheet verified at 390.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **4af — Native type scale**                     | Done                    | Phone type was oversized vs native apps. Locked tokens: title 18 · section 14 · body 14 · KPI 16 · list amount 15 · tabs 13. Covered admin chrome + guest form/auth/account/pay-parking + listing/PDP chrome + shared Card/Dialog/Sheet/AlertDialog. Marketing display heroes (film/showcase/`MarketingPublicPageHero`) intentionally stay large. Docs: `mobile-responsive` + `DESIGN.md`.                                                                                                                                                                                                                                                                                                                          |
| **4ag — Segment + search density**              | Done                    | Mobile Card/Calendar + Name/Price segments were oversized/uneven (fixed `h-7` triggers inside padded tracks). Dense segments now fill a padded track (`h-8`/`h-9`, `p-1`, `text-[13px]`); list search placeholders use `text-[13px]` on phone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **4ah — Form segment consistency**              | Done                    | Sign/Upload + GAF/Pet → shared `SegmentedControl` `dense` `fullWidth` (~32px track, 11px label). Card-header toggles (Name/Price, All/Income/Expenses, revenue/bookings) → `dense` + `equalSegments` (~28px) via `cardHeaderSegmented*ClassName`. No base typography rewrite — shared `.text-*` tokens restored to prior scale; only removed oversized `max-sm` page-title bump.                                                                                                                                                                                                                                                                                                                                    |
| **4ai — List card declutter**                   | Done                    | Phone list cards distilled: Bookings (no email; name+₱; status+dates), Finance ledger, Maintenance reminders (no notes on phone), Super-admin Approvals/Support. Shared `AdminCardGrid` phone gap `gap-2`. Desktop/sm+ card chrome unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **4aj — Type + overflow density**               | Done                    | Card title 14px phone; `text-card-description`; Card padding `p-4` phone; Team ⋯ uses `admin-overflow-trigger` (no outline boxes); Notifications Activity / push / Telegram / Install banner aligned. Docs: mobile-responsive rule + skill + DESIGN.md.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **4ak — Template WYSIWYG density**              | Done                    | Templates Edit/Preview + Placeholders/Reset match dense segments; TipTap toolbar `size-8`; phone rich-text scale (14px body / smaller H1–H3). Stay Guide page-editor section chrome matched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **4al — More sheet type density**               | Done                    | `AdminMoreSheet` nav rows: `text-[13px]` + `size-4` icons + `min-h-[44px]` (was `text-sm` / `size-5` / 48px). Theme toggle, profile, and Sign out matched to the same scale as ModeSwitcher / list chrome.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **4am — Finance/Maintenance card ⋯**            | Done                    | Phone Finance + Maintenance cards: status + meta on row 2 (Bookings pattern); edit/delete/series collapsed into `AdminListCardOverflowMenu` (⋯ → sheet). No inline pencil/trash on phone. `sm+` footer icons unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **4an — Bookings card distill**                 | Done                    | Phone booking cards: removed chevron and `Nn`/`Ng`; status on its own line (no truncation against dates); dates + flags on the next line. `sm+` unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **4ao — Straight hero edge**                    | Done + verified         | Removed the elliptical bottom arc from `.mobile-brand-hero` / `.mobile-sticky-chrome` — bottom edge is now a straight line on **all** `AdminMobilePage` routes. Scroll parallax kept (bottom pad + title still compress via `--hero-collapse`). Date-range float still straddles the green/white seam (expanded + pinned). `::after` glow flattened to a full-width seam drop. Verified 390 on org dashboard + property inbox.                                                                                                                                                                                                                                                                                      |
| **4ap — Flush hero on no-float pages**          | Done + verified         | Pages **without** a floating toolbar (no date-range picker — Inbox, Notifications, Plans, Settings, Team…) got `MobileBrandHero flush` → `.mobile-brand-hero--flush` cuts the bottom pad from `2.5rem` to `~0.9rem` (title→canvas gap 40px → 14px). Removed the arc-era `-mt-6` canvas tuck; no-float stack pad unified to `pt-3` (was `pt-5` non-fill / `pt-3` fill). Float pages keep the tall pad for the straddling toolbar. Parallax still respected (smaller travel). Verified 390 on Inbox / Notifications / Plans + scroll-collapse; org dashboard (date-range) unchanged.                                                                                                                                  |
| **4aq — Stat card weight restore**              | Done                    | KPI values looked regular-weight: `twMerge` stripped `text-stat-value` when a color/`text-foreground` followed. Fixed `cn()` to register type-scale tokens; `StatCard` no longer appends redundant `text-foreground`. Also fixed `native-icon-tile` embedding `flex` so phone `hidden` works again.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **4ar — Brand hero on property booking detail** | Done + verified         | Property `BookingDetailPage` (bespoke `<md` workflow-first layout, not `AdminMobilePage`) now renders `MobileBrandHero` (title **Booking**, `md:hidden`, parallax) so phone gets the teal header like every other page. New `[data-page-brand-hero]` marker + scoped `@media (max-width:767px)` rule drops the `AdminLayout` fallback topbar + shell gutter below `md` only; tablet/desktop untouched. `BookingDetailMobileSummary` heading → `h2` (hero owns the `h1`). Verified 390 / 834 / 1440 + scroll-collapse.                                                                                                                                                                                               |
| 5 — Doc updates                                 | Partial                 | `mobile-responsive` + **4af**/**4aj**/**4al**/**4ao**/**4ap**/**4aq**/**4ar**; bookings-detail route guide **4ar**; finance/maintenance **4am**; bookings **4an**; templates **4ak**; DESIGN.md notes twMerge + type tokens                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

---

## Context

Today the app is a responsive website: pages reflow at breakpoints, but the mobile experience is still "a desktop layout that fits on a phone" — a hamburger-triggered drawer for admin nav, centered dialogs at every viewport, hand-rolled CSS transitions in most flows, and no persistent app-like navigation outside a single top-docked account tab strip. The user wants every page — public marketing/browsing, the guest booking flow, the guest portal, and the full admin dashboard (org/property/parking/super-admin) — rebuilt so mobile feels like a native app: bottom tab navigation, native-feeling transitions/gestures, and sheet-style modals where appropriate, not just adjusted breakpoints.

This is a whole-app effort touching dozens of routes, so this plan defines the shared architecture once and phases the rollout across the app rather than speccing every page individually. Phase 0 + Phase 3 (dashboard shell) are implemented; guest/public surfaces remain for later passes.

### Locked product decisions

1. **Scope**: visual/interaction redesign only. PWA installability (manifest, service worker, offline shell) is explicitly **out of scope** — flagged as a future phase, not designed here.
2. **Bottom tab navigation** appears everywhere — dashboard admin, guest portal, public marketing/browsing, and even inside the guest booking form/calendar/sd-form/pay-parking flow. The wizard-vs-tab-bar conflict this creates is resolved below (§3).
3. **Modals**: hybrid — short confirmations/alerts stay centered `Dialog`; longer forms/detail panels/filters become bottom sheets on mobile.
4. **Animation**: framer-motion (already a dependency, `^12.42.2`) becomes the app-wide standard for page transitions, sheet/drawer open-close, list enter/exit, and gestures (swipe-to-dismiss, drag-to-close). Raw CSS transitions remain only for cheap hover/press micro-interactions.
5. **Explicit exclusion**: the `stay-guide` (`StayGuidePage`) and `showcase` (`PropertyShowcasePage`) routes are **out of scope for this entire module** — they are already responsive and intact. Do not add the shared bottom-tab shell to them, do not touch their components for touch-target/spacing/type-scale conformance, and do not include them in any Phase 1/2 sub-phase or the closing verification sweep.

## Current-state findings (verified against code, not assumed)

- **Breakpoints**: Tailwind defaults only (`sm:640 md:768 lg:1024 xl:1280`), no custom breakpoints (`ui/tailwind.config.js`).
- **A viewport-query hook already exists**: `ui/src/hooks/useMediaQuery.ts` exports `useIsBelowMd/Lg/Xl` + `usePrefersReducedMotion`, built on a shared `useMediaQuery(query)` primitive (`matchMedia` + change subscription). Reused across `BookingsListPage`, `FinancePage`, `MaintenancePage`, `MarketingNav`, etc. **No `useIsMobile()` phone-tier export exists yet** — needs adding.
- **No existing bottom tab bar anywhere in the app** (correcting an earlier assumption made mid-research). The closest precedent, `ui/src/features/guest/account/components/GuestAccountMobileNav.tsx`, is actually a **top** `sticky top-16` segmented tab strip (verified in `GuestAccountLayout.tsx:14-18`) — not bottom-docked. What _is_ genuinely reusable from it: `ui/src/components/ui/SlidingActivePill.tsx` + `ui/src/hooks/useSlidingActivePill.ts`, confirmed fully generic (pure ref/DOM-measurement, no nav-specific coupling) — this becomes the animated active-indicator for a **new** bottom tab bar, not a relocation of an existing one.
- **Admin shell** (`ui/src/features/dashboard/bookings/components/AdminLayout.tsx`): mobile (`<lg`) = sticky topbar + hand-built hamburger slide-in left drawer (custom `-translate-x-full`↔`translate-x-0`, no shadcn Sheet, no framer-motion, but does already do `inert` mgmt + scroll-lock correctly). Desktop (`lg+`) = fixed collapsible sidebar, untouched by this plan.
- **List/table → card-list precedent already shipped**: `BookingCardGrid.tsx` + `FinanceLedgerCardGrid.tsx`, wired via a `view: 'table'|'card'|'kanban'` state + existing `ui/src/hooks/useAdminMobileCardViewGuard.ts` + `hideTableView={isMobileLayout}`, live on `BookingsListPage`, `ParkingBookingsPage`, `FinancePage`. This is the template to generalize, not invent.
- **`ui/src/components/ui/sheet.tsx`** is built on `@radix-ui/react-dialog` (confirmed), already supports a `side: 'bottom'` variant in `sheetVariants`, but is **imported nowhere in the app today** — a clean slate. It gives focus-trap/scroll-lock/portal handling for free; it lacks native drag-to-dismiss (addable with framer-motion `drag="y"`, matching decision #4 instead of adding a second gesture engine like `vaul`).
- **`Dialog`** (`@/components/ui/dialog`) is used in 55 files today, universally, regardless of viewport.
- Safe-area-inset handling is an already-established pattern (`dialog.tsx`, `MainLayout.tsx`, `BookingEditStickyBar.tsx`, `StayGuideHero.tsx`) — not new ground.
- Full page/route inventory (every route grouped by surface: public marketing/browsing, host auth, operational guest flow, guest portal, super-admin, org onboarding, org-scoped, property-scoped, parking-scoped) was enumerated during research — see Phase breakdown below for how it's grouped into shippable chunks.

## Architecture

### 1. Foundation primitives (`ui/src/components/mobile/`, new directory)

- **`BottomTabBar.tsx`** — shared bottom nav primitive. Props: `items: { href, label, Icon, badge? }[]`, `activePathname`, optional `onMoreClick`. Built on `useSlidingActivePill` + `SlidingActivePill` (reused as-is). Fixed (`fixed inset-x-0 bottom-0 z-40`), `pb-[max(0.5rem,env(safe-area-inset-bottom))]`, 44px+ touch targets.
- **`BottomBarSlot.tsx`** + `BottomBarSlotProvider` — the single fixed bottom band renders exactly one of `<BottomTabBar>` (persistent nav) or `<ContextualActionBar>` (screen-owned primary action, e.g. wizard Back/Continue) at a time, via a `useContextualActionBar(node)` hook screens call to claim the slot and release it on unmount. Cross-fade via framer-motion `AnimatePresence`. This generalizes the existing `BookingEditStickyBar.tsx` pattern into a reusable slot rather than a one-off.
- **`ContextualActionBar.tsx`** — the "primary action" bottom bar consumed by wizards/detail-edit screens.
- **`MobileAppShell.tsx`** — per-surface scaffold owning content bottom-offset and `overscroll-behavior-contain` (never `overflow-hidden` on body/root, preserving the existing iOS-momentum-scroll rule). `AdminLayout`, `MainLayout`, `MarketingLayoutShell` each delegate their mobile path to this.
- **`PageTransition.tsx`** — thin `motion.div` wrapper (opacity + 8-12px slide) applied per-surface around route `Outlet`s, not globally (avoids double-animating nested layouts). Respects `usePrefersReducedMotion` (existing hook — do not add framer-motion's own `useReducedMotion`, keep one source of truth) by rendering with no animation at all when reduced motion is on.
- **`AdminCardGrid.tsx` / `AdminCardRow.tsx`** — generic card-list shell (loading/empty/error states + tappable card with keyboard support) extracted from `BookingCardGrid`/`FinanceLedgerCardGrid`'s duplicated logic, so every future list-page conversion reuses it instead of hand-rolling.
- **`useMediaQuery.ts`**: add `useIsMobile()` (phone-tier, e.g. `max-width: 639px`) alongside the existing `useIsBelowMd/Lg/Xl` exports.
- **Sheet primitive**: extend `ui/src/components/ui/sheet.tsx` in place — bottom-side polish (`rounded-t-2xl`, drag-handle affordance, safe-area padding, `max-h-[85dvh]` with internal scroll) + framer-motion `drag="y"` for dismiss. New **`ui/src/components/ui/bottom-sheet.tsx`** (thin re-export defaulting `side="bottom"`) and **`ui/src/components/ui/responsive-modal.tsx`** (renders `Dialog` above `sm`, `BottomSheet` below — the thing most of the 55 Dialog call sites migrate onto instead of each hand-rolling a breakpoint check). **Decision: do not add `vaul`** — it would run a second, independent gesture/animation engine alongside framer-motion, contradicting decision #4; Sheet-on-Radix already gives the a11y primitives vaul would otherwise re-solve.

### 2. Mobile shell per surface

- `AdminLayout.tsx` mobile path rebuilds on `MobileAppShell` + `BottomTabBar` + `BottomBarSlot`; the hamburger drawer's content becomes a "More" bottom sheet instead of a left-slide panel. Desktop (`lg+`) fixed sidebar is untouched.
- `MainLayout.tsx` (guest operational flow) gains a `BottomBarSlot`; its current floating corner buttons (admin entry/theme toggle) get normalized into a slim top bar so it isn't floating-buttons-plus-tab-bar.
- `MarketingLayoutShell.tsx` gets bottom tabs (Explore/Properties/Parkings-Developments/Account) + a "More" bottom sheet for secondary links (For Hosts, Services, Terms, Privacy, theme toggle); `MarketingNav.tsx`'s current full-screen hamburger `AnimatePresence` overlay is retired, not kept alongside the new tab bar (two nav paradigms stacked is exactly what decision #2 must avoid).
- Guest account (`GuestAccountLayout.tsx`) keeps its existing top segmented strip conceptually but is migrated to consume the new shared `BottomTabBar`/`BottomBarSlot` at the bottom instead, for consistency with every other surface (this is a real behavior change for guest account, not just a refactor, since it moves from top to bottom placement) — call this out explicitly to the user during Phase 2 implementation as a visible change from what exists today.

### 3. Booking-form-flow bottom-nav resolution

**Rule**: screens with a single dominant linear action or multi-step flow claim the `ContextualActionBar` and the tab bar is unmounted for that route; browsing/list/dashboard screens with no single dominant action show the persistent tab bar. Same rule resolves PDP CTAs, edit-form Save/Cancel, and payment forms — one rule, not one exception per screen type.

| Route                                                         | Bottom band                                                                                                                                                 |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `calendar`, `messages`                                        | Tab bar                                                                                                                                                     |
| `stay-guide`, `showcase`                                      | **Out of scope** — already responsive/intact, do not modify                                                                                                 |
| `form` (`GuestForm.tsx` wizard)                               | `ContextualActionBar` = promoted `GuestFormStepNavigation.tsx` Back/Continue (fixed, safe-area padded, same upgrade `BookingEditStickyBar.tsx` already has) |
| `sd-form`, `guest-review`, `parking/:bookingId` (pay-parking) | `ContextualActionBar` (verify each has a single dominant submit/pay action during implementation)                                                           |
| `success`                                                     | Tab bar reappears                                                                                                                                           |

Rationale: stacking a 56px tab bar + a 56-64px Back/Continue bar burns 112-120px+ safe-area on the narrowest supported viewport (375px) on a form already tight on space — time-slicing the same band costs zero extra space and matches checkout-flow mental models users already know. While the tab bar is hidden, the top area gets an explicit exit affordance ("X" / "Back to Calendar") so users aren't stranded.

### 4. List/table → mobile card-list conversion

Every remaining table page (Team, Maintenance, Templates, super-admin Developments/Approvals/Hosts/PlatformProperties, org Properties/Parkings) adopts the same 3-part pattern already proven on Bookings/Finance: (1) `view` state + `useAdminMobileCardViewGuard`, (2) a `<Domain>CardGrid.tsx` built on the new `AdminCardGrid`/`AdminCardRow` shells, (3) `hideTableView={isMobileLayout}` passed to the existing table. Mechanical work once the shared shell exists (Phase 0) — not new architecture.

### 5. Dialog → hybrid modal conversion rule

Converts to bottom sheet (via `ResponsiveModal`) when **any** of: >~3 fields or a rich/file-upload field; it's a picker/list; it's a full read+edit detail panel; it's opened from an already mobile-optimized flow. **Stays** a centered `Dialog` when it's a short confirm/alert (destructive confirms, single-field prompts, brief informational dialogs).

Rough sweep of the 55 sites, by module (destructive confirms and single-field prompts stay Dialog everywhere; forms/pickers/detail panels convert):

- **Inbox**: quick-reply/AI-response/page-picker dialogs → sheet; `InboxMediaPreviewDialog` → special-cased as a full-bleed lightbox, not `ResponsiveModal`.
- **Bookings**: `PayParkingModal`, `BookingKanbanWorkflowModal`, `PropertyIntegrationsPanel`, Telegram/template config dialogs → sheet; `TelegramHelpDialog` (short info) → stays Dialog.
- **Org**: `GetVerifiedModal` (multi-step) → sheet; `AddEntityDialog`/`OrgDangerZoneSection` (destructive) → stays Dialog; `PropertyMediaPreviewDialog` → lightbox special case.
- **Team**: invite/edit-permission dialogs → sheet; `RemoveMemberDialog` (destructive) → stays Dialog.
- **Finance/Maintenance**: `RecurringSeriesModal`/`StayFinanceModal` → sheet; `RecurringDeleteDialog` (destructive) → stays Dialog.
- **Marketing (dashboard)**: `PublishDialog`/move-template picker → sheet; `MarketingNameDialog` (single field) → stays Dialog.
- **Parking/Pricing**: booking/settings forms → sheet; `PricingSaveDialog` (single confirm) → stays Dialog.
- **Guest-facing**: `ContactHostSheet` (already named "Sheet" but built on `Dialog` today — first to migrate onto the real primitive); `GuestAuthModal` → flagged as a UX call to make during implementation, default to keeping centered (trust/auth convention); `VoiceSessionPanel` excluded (inline conversation replacement, not a hybrid modal candidate); `PropertyRules`/`PropertyAmenities` → sheet; `BookingCalendarModal` → sheet.

Net: roughly 35-40 of 55 sites convert; ~15 (destructive confirms, single-field prompts, 2 media lightboxes, auth modal) stay Dialog everywhere.

## Phased rollout (each phase independently shippable)

- **Phase 0 — Foundation**: build everything in §1 (no visible route changes). Refactor `BookingCardGrid`/`FinanceLedgerCardGrid` onto the new `AdminCardGrid`/`AdminCardRow` shells to prove the extraction against two already-shipped consumers before anything new depends on it. No skill-doc edits yet.
- **Phase 1 — Guest booking flow** (highest traffic, highest risk — `GuestForm.tsx` is 2,696 lines, verified 2026-09-05), split into two independently-shippable sub-phases:
  - **1a — Guest operational shell**: `MainLayout` gets `BottomBarSlotProvider` + `MobileAppShell`; floating corner buttons (admin entry/theme toggle) normalized into a slim top bar; persistent tab bar shown on `calendar`/`messages`/`document` (read/browse screens, no dominant action). `stay-guide` and `showcase` are explicitly **excluded** from this shell rollout — already responsive/intact, do not modify.
  - **1b — Wizard → `ContextualActionBar`**: `GuestFormStepNavigation` promoted to `ContextualActionBar` across `form`/`sd-form`/`guest-review`/`parking/:bookingId` (verify each has one dominant submit/pay action first); `success` returns to the tab bar; add an explicit exit affordance ("X"/"Back to Calendar") while the tab bar is hidden mid-wizard. Ship and Playwright-verify alone before touching anything else — highest-risk single change in this plan.
- **Phase 2 — Guest portal + public marketing/browsing**, split into six independently-shippable sub-phases (shells first, since one shell change covers many routes at once):
  - **2a — Marketing shell**: bottom tabs (Explore/Properties/Parkings+Developments/Account) + "More" sheet on `MarketingLayoutShell`/`MarketingNav`, retiring the hamburger `AnimatePresence` overlay entirely. Covers `/` landing, `/search`, `/for-hosts`, `/for-hosts/pricing`, `/services`, `/hosts/:orgSlug`, `/properties(/in/:location)`, `/parkings(/in/:location)`, `/parkings/requests/:bookingId`, `/developments(...)`, `/about`, `/contact`, `/support`, `/terms`, `/privacy`, `/cookies` in one change — no per-page shell work needed.
  - **2b — PDP CTAs → `ContextualActionBar`**: `PropertyDetailPage`, `ParkingDetailPage`, `DevelopmentDetailPage`, `HostPublicPage` primary CTA (Book now / Request parking / Contact host).
  - **2c — Guest portal shell migration**: `GuestAccountLayout`/`GuestAccountMobileNav` top `sticky top-16` strip → shared `BottomTabBar`/`BottomBarSlot` (top→bottom placement change, call out explicitly as a visible behavior change, not just a refactor), reusing `SlidingActivePill`/`useSlidingActivePill`. Covers `/account/profile`, `/stays`, `/vouchers`, `/favorites`, `/tickets/*`.
  - **2d — Auth pages**: `AuthLayout` (`sign-in`, `for-hosts/login`, `for-hosts/register`, `for-guests/login`, `for-guests/register`) default to **no tab bar** (linear trust-flow convention) — bring up to touch-target/spacing/type-scale rules only; confirm the no-tab-bar default holds during implementation rather than assuming.
  - **2e — Guest-facing modal sweep** (per §5 above): `ContactHostSheet` → real `ResponsiveModal`/sheet primitive; `PropertyRules`/`PropertyAmenities`/`BookingCalendarModal` → sheet; `GuestAuthModal` stays centered; media/document lightboxes stay full-bleed special cases.
  - **2f — Property-scoped read-screen polish**: `document`/`messages` already inherit the 1a shell — this is touch-target/spacing/type-scale conformance only, not new shell work. `stay-guide` and `showcase` are excluded from this whole module (see Locked product decisions) — do not touch either page or its components.
- **Closing audit**: before calling the whole-app rollout done, (1) close out or explicitly re-defer the two items this doc still marks incomplete on the dashboard side — **Team permissions matrix** mobile card view (Phase 4) and **Phase 5 doc updates** (partial) — and (2) run the full Playwright viewport sweep across every route in the public inventory above, including ones not directly touched by 1a-2f (`NotFoundPage`, legal pages, `ParkingRequestStatusPage`), to catch anything outside this list.
- **Sequencing**: per this repo's "one task ≈ one session" rule, each sub-phase (1a, 1b, 2a-2f) should be its own session/PR, the same way Phase 3/4 actually shipped as 40+ discrete commits (4a-4ar) rather than one mega-change. Do not attempt 1a-2f in a single sitting.
- **Phase 3 — Dashboard admin shell**: `AdminLayout` mobile path rebuilt on `MobileAppShell`/`BottomTabBar`/`BottomBarSlot`; tab items prefer Dashboard / Bookings / Inbox, then **Notifications** and **Assistant** overlay actions + "More" sheet for the rest (Finance under More). Desktop sidebar has no bell — notifications float above the AI FAB. `BookingEditStickyBar` migrated onto `ContextualActionBar`.
- **Phase 4 — Remaining dashboard modules**: list/card conversions for every table page not already covered; Dialog → `ResponsiveModal` sweep per §5, splittable into sub-PRs per module group (mechanical, low interdependency).
- **Phase 5 — Doc updates**: amend `.cursor/rules/mobile-responsive.mdc` + mirrored `.claude/skills/mobile-responsive/SKILL.md` — admin shell diagram (3-zone: topbar/content/`BottomBarSlot`, hamburger retired), modal guidance reversal (hybrid rule replaces "avoid full-screen sheets"), new "Bottom navigation & contextual bars" + "Animation standard" sections, updated testing checklist. Landed last, once shipped behavior is the source of truth. Note PWA as an explicit future follow-up, not built here.

## Verification approach (no automated test suite in this repo)

Per phase: `bun run type-check` / `lint` / `build` clean first, then manual Playwright-driven viewport passes at 375/390/768/1024/1440px (`browser_resize` + `browser_navigate` + `browser_snapshot`/`browser_take_screenshot`) checking: tab bar and contextual bar are never both visible, sheet drag-to-dismiss doesn't fight page scroll, safe-area padding present on new fixed-bottom elements. Phase-specific smoke flows: Phase 1 drives the full booking wizard end-to-end at 375px; Phase 3 confirms the "More" sheet surfaces every permission-filtered nav item the old drawer did, per tenant type; Phase 4 spot-checks card-grid data parity against the table view it replaces. Include one dark-mode and one reduced-motion-emulated pass per phase. Phase 0's refactor gets an explicit regression check on the _existing_ Bookings/Finance card views before any new consumer is built on the extracted shells.

## Critical files

- `ui/src/features/guest/account/components/GuestAccountMobileNav.tsx` + `ui/src/components/ui/SlidingActivePill.tsx` + `ui/src/hooks/useSlidingActivePill.ts` — reusable primitives for the new `BottomTabBar`; `GuestAccountMobileNav`/`GuestAccountLayout.tsx` are also the Phase 2c migration target (top strip → shared bottom bar)
- `ui/src/layouts/MainLayout.tsx` — guest operational shell, Phase 1a
- `ui/src/features/guest/form/components/GuestFormStepNavigation.tsx` / `GuestForm.tsx` — first `ContextualActionBar` consumer, Phase 1b
- `ui/src/features/guest/marketing/shared/components/MarketingLayoutShell.tsx` / `MarketingNav.tsx` — Phase 2a shell rebuild (hamburger overlay → bottom tabs + More sheet)
- `ui/src/features/guest/marketing/pages/PropertyDetailPage.tsx` / `ParkingDetailPage.tsx` / `DevelopmentDetailPage.tsx` / `HostPublicPage.tsx` — Phase 2b `ContextualActionBar` CTA consumers
- `ui/src/features/guest/marketing/shared/components/GuestDialogShell.tsx` — guest-side `ResponsiveModal` wrapper; `ContactHostSheet`/`PropertyRules`/`PropertyAmenities`/`BookingCalendarModal` migrate onto it in Phase 2e
- `ui/src/features/guest/auth/components/AuthLayout.tsx` — Phase 2d touch-target/type-scale pass (no tab bar by default)
- `ui/src/features/dashboard/bookings/components/AdminLayout.tsx` — admin shell mobile rebuild, Phase 3
- `ui/src/features/dashboard/bookings/lib/adminSidebarNav.ts` — existing nav-section builders to source bottom-tab items from
- `ui/src/components/ui/sheet.tsx` — extended in place (not replaced by `vaul`)
- `ui/src/features/dashboard/bookings/components/BookingCardGrid.tsx` + `FinanceLedgerCardGrid.tsx` — precedent generalized into `ui/src/components/mobile/AdminCardGrid.tsx`/`AdminCardRow.tsx`
- `ui/src/hooks/useMediaQuery.ts` / `useAdminMobileCardViewGuard.ts` — extended, not replaced
- `.cursor/rules/mobile-responsive.mdc` + `.claude/skills/mobile-responsive/SKILL.md` — Phase 5 amendments
